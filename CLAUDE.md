# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository

- **GitHub:** https://github.com/Shu6hamSingha1/Soft-Precise-Landing
- **Remote:** `git@github.com:Shu6hamSingha1/Soft-Precise-Landing.git`
- **Default branch:** `main`

## Git Workflow

After every file change, commit and push to GitHub:

```bash
export PATH=$PATH:"/c/Program Files/GitHub CLI"
git add <files>
git commit -m "descriptive message"
git push origin main
```

## Environment

- Platform: Windows 11, shell: bash (Git Bash / MSYS2)
- GitHub CLI (`gh`) is installed at `C:/Program Files/GitHub CLI`
- SSH authentication is configured (`~/.ssh/id_ed25519`) and connected to GitHub account **Shu6hamSingha1**
- Git global config: `user.name = Shu6hamSingha1`, `user.email = shubhamsing2@iisc.ac.in`
