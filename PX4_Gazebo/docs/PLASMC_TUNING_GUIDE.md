<!--
  PLASMC PARAMETER-TUNING GUIDE — the single entry point for any tuning / landing-diagnosis work.
  A SessionStart hook (.claude/settings.json) injects the STATUS block below into every session and
  tells the assistant to read this whole file before tuning. KEEP THE STATUS BLOCK CURRENT — update it
  (and the rest) at the end of every tuning round. This file is the orientation layer; it points to the
  deep docs (§11) rather than duplicating them.
-->

# PLASMC Parameter-Tuning Guide — START HERE

<!-- STATUS:BEGIN -->
## ⏱ STATUS (last updated 2026-06-14 — keep current)
- **🟢 2026-06-14 — CBF (cbf2) ISOLATED + VALIDATED + SITL-CONFIRMED → 2 bugs fixed, BOTH DEFAULT-ON.** cbf2 lifted into pure `src/cbf_visibility.py` (parity Δ=0); offline validator `tools/validate_cbf.py` (13/13). **Fix A (`CBF_LW_ROT`, default-ON):** the QP fed `L_ω` the LEAN vector `θ` but `L_ω` couples the ROTATION-axis vector — 90° off (231%→8.7% model err vs an independent camera); fix = `Lw2 @ [[0,1],[-1,0]]`. **Fix B (`CBF_RD3_DIRECT`, default-ON):** `θ_safe` was laundered tilt→accel→LPF(α≈0.83)→tilt; now `rd3=[−Rz(yaw)·θ_safe,1]` built directly (vertical thrust still from loom). **✅ SITL A/B (IC2 4-cell n=5, `tools/analyze_cbf_visibility.py`): on the CBF's OWN metric (target visibility, NOT fly-away), Fix A ~doubles marker-in-FoV under tilt (hitilt_vis 0.24→0.43–0.45) + halves runaway tilt (max 99°→51–62°); `LW_ROT=1` arms cluster vs `=0`. Fix B visibility-neutral but best combined margins.** Fly-away is a CONTROL-tuning issue the CBF can't fix (judge CBF by visibility only). See [[feedback_cbf_lw_rotation_bug]], `docs/FUNNEL_CBF_DESIGN.md` top addendum.
- **🔵 2026-06-14 — DESCENT CHAPTER CLOSED + LATERAL WALL ROOT-CAUSED → now tuning LATERAL.** See [[feedback_lateral_overshoot_root]] [[feedback_descent_perception_ceiling]].
  - **DESCENT closed:** every z-lever exhausted. z-gain sweep (KAPPA0/N/OMEGA/XI2/P20/E, 11 probes) — all no-help or regress; the loom `h_z` UNDER-REPORTS the descent velocity at PLASMC speed (LK dynamic-range, corr ~0 below 2 m), so the z-SMC regulates a dead signal — no gain fixes it. Perception is at its ceiling on a clean descent (decode 88-100%, corners survive, marker never fills FoV, NO Gazebo motion blur) — LK/KLT/ArUco/deblur params all NO-effect (`tools/tune_lk_survival.py`, `tools/tune_aruco_decode.py`). Loom recal `[2,2]=1.2988` was A/B-NEUTRAL → **reverted to 1.0744**. The lever for descent softness is `h_rd` (govern velocity into the observable range) — UNBUILT.
  - **LATERAL wall ROOT = OVERSHOOT from under-reported velocity.** At IC2 the drone CONVERGES in position (2.6m→0.8m by alt ~2m, outer PID + `s_e_n` input both GENUINE) then OVERSHOOTS — arrives with ~3 m/s lateral velocity, flies THROUGH the target → runaway → fly-away. Cause: lateral flow `h_y` UNDER-REPORTS the lateral velocity (2-10× under) — SAME root as the descent loom. **The PID isn't the problem; the inner-loop VELOCITY feedback (`h`) is under-reported → under-brakes → `s_e` overshoots 0.**
  - **`cross(w_i,s)`/`w_iz` was a SYMPTOM, not the cause.** The overshoot tilts → off-screen virtual `s` + `_sensor_cal_hw` row-5 cross-coupling (`+1.0862·h_y`, `−0.7395·KF(w_x)`) fabricated `w_iz`≈−1.5 (raw yaw clean ~0). **FIXED (valid cleanup): w_z FULL-decouple `_sensor_cal_hw[5]=[0,0,0,0,0,1.0151]`** (`PLASMC_WZ_CROSS=1` reverts) — `w_iz` now GT-clean, BUT fly-away persists → confirms symptom not cause. (Old `PLASMC_VIRT_GUARD` s-clamp also regressed.)
  - **First lever that HELPED: `XI2_xy=0.8`** (lateral flow-funnel contraction, up from 0.6) → IC2 n=5 mean xy **26.5→13.1, 2/5 close** (vs 1/5), 3 TL. Mitigates the overshoot (tighter funnel) but reps still land hard + 3/5 fly away. **NEXT (new chat): push `XI2_xy`, the other lateral SMC/PID/PPC gains, and a LATERAL VELOCITY GOVERNOR (cause-side, analog of `h_rd`).**
  - **⚠️ Baked z-config is the `Ez5_combo` user-bake (`XI2_z=0.8 P20_z=10 OMEGA_z=0.1 E_z=0.5`) — a TAME-z baseline for lateral work, NOT the soft optimum** (which was `E_z=0.1` etc., vel 1.3-1.8). Ez5_combo regressed soft descent (IC2 n=5: 4/5 TL) but its z-loop doesn't over-brake (won't confound lateral debugging). Revert via `PLASMC_*_Z` env for soft descent.
- **🟢 2026-06-13 SOFT-CONFIG BAKE — first soft+precise of the campaign.** On top of the Combo, baked: `XI2_z=0.6 · P2INF_xy=1.5 · OMEGA_xy=0.1 · GAMMA=2.0/2.0/1.0 · E=0.8/0.8/0.1 · N_z=0.1 · KAPPA0=0.5/0.5/1.0`. n=5 IC2(2,2,5): **3 clean reps soft-precise (xy 0.85–1.50, vel 1.66–2.44 m/s)** + 1 lateral-wall TL + 1 flake. **KEY: `KAPPA0_Z=1.0`** (the bootstrap value — Combo had reverted it to 0.25) gives z braking authority from t=0 → soft touchdown (vel 2.84→1.3–1.8); **`E_z=0.1`** = crisp arrest but un-bounds κ_z ALONE — only stable PAIRED with κ0_z=1.0 (no ratchet). `GAMMA_xy=2.0` = the un-saturated reaching gain that actually speeds `h_e` (κ_0/E are sat-gated). The descent-softness gap (hard 3–4 m/s touchdown = `h_rd`/1-Z velocity overshoot) is what this fixed. ⚠️ NOT gate-clean (IC2-5 not run; the 1 TL is the known lateral perception wall, config-independent). See [[feedback_plasmc_two_task_framework]]. ODS NC77.

- **Platform:** PX4 SITL + Gazebo Harmonic, `x500_mono_cam_down`, ArUco world, 640×480 down-cam. MATLAB phase done; PX4 phase active. **Cal regime = R3 (honest 8-run cal, `d60973a`).** Results only compare *within* a regime.
- **Baked defaults (`src/controller.py`, R3 — SOFT-CONFIG + last-config bake 2026-06-13):** `K_rp=5 · K_ri=0.1 · K_rd=0.5 · gamma_s(XIS)=0.5 · PSINF=0.35 · XI2=0.6/0.6/0.6 · P=1.5/1.5/5 · E=0.8/0.8/0.1 · Γ=2.0/2.0/1.0 · Ω=0.1/0.1/0.025 · N=0.02/0.02/0.1 · KAPPA0=0.5/0.5/1.0 · KAPPA_MAX_Z=3.0 (reverted from 10) · p2inf=1.5/1.5/0.5 · FLOOR=60 · cbf2 · SEN_FUNNEL=1 · W_U_MAX=1.0 · KR=1.5/1.5/0.5 · YAW_PSID_RATE=1.0 · BODY_YAW_SOURCE=alpha · CTRL_ZERO_WXY=1 · DH_D_MAX=50 · h_rd=−0.3 · IMG_FEATURE_FILTER=kf · MARKER_KLT_MAX_STEPS=20`. ⚠️ **NOT gate-clean** (see gate result below). ⚠️ `h_rd=−0.3` carries a LATERAL cost (slower descent → longer lateral-wall exposure); `E_z=0.1` stable only with κ0_z=1.0.
- **🟢 2026-06-13 IC2-5 GATE on the baked soft config (NC79) — MAJOR improvement vs Combo, still not clean.** n=5 (19 valid, 1 flake): **17 land / 2 TL / SP 0.** vs the COMBO gate (14/20, 6 TL, 27 m catastrophe): **TL 30%→~10%, worst xy 27.5→16.8.** **IC5 (the Combo's catastrophe cell, 3 TL) is FIXED — 4/5 land, vel 1.36–2.19 soft** (κ0_z=1.0 helped the low-alt cell). Per-IC: IC2 4/5(1 TL 16.8), IC3 4/4(+flake), IC4 5/5, IC5 4/5(1 TL 9.0). Softness held: vel<2.5 in 10/19, vel<1.5 in 4/19. **NOT clean:** SP 0 (xy_med 1.5–3.2 too loose for *precise*; vel_med 2.2–4.9 above *soft*), 2 TL = the residual lateral wall (~10%). **`KAPPA_MAX_Z=10` CONFIRMED net-negative → REVERTED to 3.0** (κ_z ran to 10 in the drift reps IC3_rep4/IC4 where 3.0 holds; clean reps sit at κ_z~1). Remaining gap: **precision + the lateral wall** (architecture).
- **🔴 2026-06-12 — COMBO BAKED (working baseline) + OMEGA terminal-cascade lever.** The IC=2 gain-chain stack (c5f9989) n=5 validation: **SP 0/5 both ICs**, every rep converges the approach then blows up at the **terminal perception wall** ([[feedback_ic2_lateral_gain_chain]] items 9–10). `PSINF=0.6` **DEAD-END** (terminal `s_e` re-grows to ~1.5 rad ≫ any funnel floor → zeta_1 still saturates). A KD/XI2/KP n=1 sweep → the **COMBO** (`KP=5 + KD=0.5 + XI2=0.6` on a MATLAB base `E=1/P=1.5/KAPPA0=.125/GAMY=.5/Nz=.05/KR=1.5/h_rd=−.42`) **beats baked at single-axis IC** (IC2 3.85 vs 8.42 m, no catastrophes) because **KP=9 was OVER-GAINED** — it amplifies the (ungainnable) close-range spurious-flow perception event into a fly-away; **KP=5 halves the demand → cascade bounded → recovers**. **COMBO baked as defaults (user decision).** BUT **IC2-5 GATE FAILED** (NC68: 6/20 TL, SP 0, 27 m catastrophe at low-alt IC5 — the single-axis win did NOT generalize; KP=5's containment is altitude-dependent). **`OMEGA_XY 0.05→0.1` — DEAD-END (NC70-71):** n=1 looked best-ever (calm terminal theta 11.8/a_u 10.1) but n=5 made BOTH worse — IC2 0→1 TL (+24.7 m), IC5 3→4 TL — faster κ-leakage bleeds κ too low → SMC under-corrects → drift (cousin of the N_XY dead-end; no κ-rate knob fixes the wall). Omega stays 0.05 (env-test only, NOT baked). Binding constraint unchanged = **terminal perception wall** (decode/corner-conditioning through the close-range tilt) — architecture, not gains.
- **Pending task:** the **descent-hover thread is closed (2026-06-10)** — see [[feedback_descent_hover_thread]]. **BAKED: cleaner image-rate `dw`** (commit 85e1011; tidies the θ_norm peak, integrator-safe, no regression at E_z=1.0 κz=1.00). The 1/5 descent **hover** is fixed by `E_z=0.5` BUT that un-bounds κ at touchdown (**one-knob-one-job**: E_z does descent stiffness AND κ-bound) → tried the κ-bound lever **`P_z`**: **`E_z=0.5 + P_z=8` TESTED + FAILED (NC55): 1 TL, mean 7.77 m — the runaway is LATERAL (κ_xy→7.26, a_u_xy→631), which P_z (z-only) cannot touch** (E_z's faster descent amplifies the close-range 1/Z lateral error). **DEAD-ENDS:** dw source-fix (image-rate-HOLD poisons the κ-integrator → 3/5 runaways) + sustained-high-θ κ-freeze (mis-targeted) + E_z=0.5+P_z=8 (runaway is lateral). **IC2-5 still pending** (no clean IC1 config yet).
- **Best so far:** `E_z=0.5` (+ cleaner-dw) → **5/5 reliable IC1 landings, mean 1.97 m, 0 TL** at n=5 (bundle `DescentEZ05_kfreeze_IC1`); the n=10 2-cell sweep then showed 2/10 lateral-drift reps → **NOT yet clean**. trial-49 best **0.75 m** clean (rep3). ⚠️ **NO verified SP in saved R3 data** — the "0.03 m" was a frozen-GT false positive (sanity-check every SP, §8).
- **`gamma_s>1.0` + `KP=12` SWEPT (NC56-60, 2026-06-10, IC1 n=5):** `gamma_s=1.2` → **0 TL** (vs baseline's 2) but **1/5 hover** (over-centers → σ_z weak); `gamma_s≥1.4` degrades (1.4 catastrophic 8.7 m) — ceiling = descent-weakening, not a demand-breach. **`KP=12+E=1.5` → tightest landings of any cell (0.34 m best ever, no t=0 LK collapse) but 1/5 the touchdown lateral breach fires harder (κ_xy=0.85, a_u_xy=521) → next = `KP=12 + κ_xy cap`.** Neither cleanly beats baseline at n=5. The binding TL cause (re-cat by ACTUAL centroid 2026-06-10) = **ArUco DECODE-fail with the marker IN-FoV** (9/12 TLs had 4/4 corners in-FoV — NOT geometric loss); the detection-loss TRIGGERS the κ-runaway (loss precedes runaway 0.80<0.88). The **lateral κ-runaway = a WRONG `h_d` from the off-screen VIRTUAL centroid** (controller uses VIRTUAL `s`, CBF uses ACTUAL; `_getVirtualPts` z_v→0 divide reprojects an in-FoV feature off-screen under tilt → `cross(w_i,s)` fabricates h_d≈−8; κ_xy UNCAPPED) — UNBOUNDABLE by gain. The clamp-`s` fix was IMPLEMENTED + partly reverted (2026-06-10): **#2 (global `_getVirtualPts` clamp, `PLASMC_VIRT_GUARD`) REGRESSED** — it clamps the GENUINE in-FoV `s` when off-center → SMC under-corrects → far drift 29/91 m (VirtGuard_EZ_IC1; the far-drift reps did NOT lose the marker) → **OFF**. **#1 (marker-LOST extrapolation clip, `PLASMC_FEAT_FOV_CLIP`)** bounds only the *phantom* → **ON** (re-test FeatClip_EZ_IC1 pending). **Real lever = keep the marker DECODED (KLT corner-track), NOT clamping the feature**, see [[feedback_lateral_kappa_runaway]] [[feedback_marker_detection_stale]]. Ultimate lever: **code-level perception** (ArUco decode robustness / KLT corner-tracking) + the IC-rig compass-drift fix. **FeatClip_EZ_IC1 (#1-on/#2-off): E_z=1.0 0 TL/1.46 (no regression); E_z=0.5 3 TL incl rep2 κ_xy=5.07 — deep root: a SPURIOUS measured flow `h`** (−6 rad/s vs GT −0.65) breaches the lateral funnel, which **multiplies** the off-screen-`s` θ_norm → κ_eq=θ·G·\|σ\|/P=505 → a_u=908. The spurious `h` = the **6-DOF corner lstsq going ill-conditioned when `n_flow_corners`→4** (single planar marker → planar translation/rotation ambiguity → \|h\| spikes to 14 vs ~2 at n≥12); the fusion EKF can't catch it (fixed R=0.05 trust; `corner_conf` keys on KLT-depth not conditioning + 0.02 floor → 5:1 at best). **FIX (committed 38fe76e): `FLOW_NCORN_SWITCH` (default 4) — n≤4 → corner NOT-ok → EKF RING branch carries `h`** (offline: ring \|h_xy\|=0.06 vs corner 13.89); centroid `s` stays corner-based. Raise toward 8–12 if mid-range (n=5–11) spikes persist. Re-test `FlowSwitch_EZ_IC1` **pending**. **So the entire κ-runaway is a close-range PERCEPTION breakdown** (centroid `s` off-screen via z_v→0 + flow `h` ill-conditioned via n→4 + ArUco decode-fail), NOT control — which is why every control-side lever was neutral/negative. **FLOW-h FIXES NOW EXHAUSTED (5 tried, 2026-06-10):** #2 global clamp (under-correct), n-switch→ring (FlowSwitch_EZ_IC1: ring has ~0 lateral → E1.0 0→4 TL), EKF innovation gate (FlowGate_EZ_IC1: regressed both — self-defeats on sustained spike via P-growth + over-rejects genuine flow → 40.7 m drift; FLOW_GATE default 1e9 OFF), GFT-density (GFTdense_EZ_IC1: `IMG_GFT_QUALITY=0.003` did NOT add corners — a clean ArUco has a FIXED corner set, n still drops to ≤4, spurious h still max 14; E0.5 0 TL/1.69 was a stochastic good draw, root metric unchanged); #1 FEAT_FOV_CLIP kept-neutral. **The spurious `h` can't be filtered OR enriched at the flow layer — ROOT = clean-ArUco corner starvation at low alt (perception front-end).** **Pyramidal-LK TESTED + FAILED (PyrLK_EZ_IC1, LK_MAX_LEVEL=4 LK_WIN=31): no improvement, `n_flow_corners` %≤4 UNCHANGED (27–40%), spurious `|h|` still max 14.** So the low-alt n-drop is **DETECTION-limited** (detector finds ~4 corners), not tracking — better LK can't track corners never detected. **Both code-only corner-enrichment levers (GFT-density + pyramidal-LK) are now EXHAUSTED.** The ONLY remaining lever is **TARGET DESIGN** (textured / multi-marker → more *detectable* corners at touchdown FoV-fill; see [[project_landing_target_design]]). LK env-tunable kept (default `maxLevel=3`). **BEST baseline = FeatClip E_z=1.0 0 TL/1.46** (#1-on, gate-off, n-switch-off; baked).
- **🟢 2026-06-11 — BASELINE RESTORED + TERMINAL-LOOM ROOT + RING-LOOM PROTOTYPE:** marker back on **`aruco_board`** + the 6×6 cal RESTORED; **all session guards removed** from img_data (pre-guard baseline); `IMG_RECORD=1` video **fixed** (frameSize transpose). Textured-marker thread **closed** (planar translation↔tilt ambiguity — not corner count — limits h_x/w_x/w_y; the tilt-only cal sweep under-excites translation → centroid cal un-derivable). **Terminal-descent root** ([[feedback_terminal_descent_loom_overreport]]): the descent tracks `h_rd=−0.42` perfectly to ~1 m, but below ~0.5 m the **corner loom over-reports ~4×** → z-SMC over-brakes → **BALLOON** instead of touchdown → drift → marker loss. The **ring divergence is the cleaner terminal loom** (planar-observable — the opposite of the lateral). **`RING_LOOM_NCORN=12` prototype (f194989):** corner loom-row R→1e6 at low n_corn → ring carries the vertical, corner keeps the lateral. Single-run **xy 1.86→0.41 m, balloon gone, marker kept**; ⚠️ the prototype's NameError (`flow_pts_1` unbound on dropout frames) truncated every buggy-run flight at its first zero-corner frame (classified MID-AIR; fake 'SOFT' = hovering at 5 m) — fixed (3267600); **EVAL RULE: reject any summary row with flight_s≈0 or an empty GT trajectory.** **HONEST n=5 (RingLoomFix_IC1, all full-GT valid): mean 12.2, 2 TL — both LATERAL fly-aways (~28–30 m) the vertical-only ring-loom can't touch; landed 0.62/1.44/1.65 incl. 2 clean touchdowns (0.24/0.44 m/s).** The vertical mechanism is real (single full run: no balloon, xy 0.41) but NOT a net n=5 win — `RING_LOOM_NCORN` stays env-gated, NOT baked. The binding lever is back to the LATERAL terminal (convergence-ordering).
- **🔗 2026-06-11 EVE — IC=2 LATERAL GAIN CHAIN ([[feedback_ic2_lateral_gain_chain]]):** four stacked gain defects diagnosed with full-flight logs (the controller's re-init **log-wipe is FIXED** — logs persist until failure): γ₂(XI2)=0.2 keeps the SMC **asleep** all approach (σ≈5–20% of E; κ leaks; MATLAB γ₂=1.2) → `XI2_XY=0.6`; κ unarmed at crossing → `KAPPA0_XY=0.5`; γ_s=1.0 **overtakes** the error → ratio saturation → **demand starvation** (check `r=s_e_n/p_s` before believing any "damped hold") → `XIS_X=0.5`; `K_ri=1.0` is **10× MATLAB** → windup pushes ds_d=+1.4 through center → `KI_X=0.1` (reversal fixed); **K_rd=0 vs MATLAB 0.5031 = NO PHASE LEAD** (Γ↑ counter-proved: +10 kick at center — the middle loop is a velocity tracker) → `KD_X=0.5`: **overshoot 5× smaller, first damped ring-down**. **STACK BAKED (c5f9989): KD_XY .5 / KI_XY .1 / XIS_XY .5 / PSINF_XY .35 / XI2_XY .6 / κ0_XY .5 / h_rd −.25 — now code defaults.** Post-bake: dh_d spikes = numerical rate of the GENUINE `cross(w_i,s)` feedforward (gyro-verified) — **capping rejected; structural ḣ_d fix open**; post-crossing plateau = the **saturated-barrier demand ceiling** (∝p_s·KP·ζmax, ds_d pinned −0.8) → `PSINF_XY=0.6` lifts it (n=1 ✓, candidate). **VALIDATION DEBT: n=5 IC2 + n=5 IC1 regression + IC2-5 gate.** Non-gain walls: decode-through-maneuver + terminal <0.5 m. `h_rd=−0.30` validated n=1 at IC=0.5 (descent-phase ignition gone). Duty-cycle staleness reverted to legacy (global-strict → 21–23 m ballistic aborts); prox-aware variant half-wired (inert).
- **Hard rules:** never run SITL yourself (GUI-bound — the user runs it); use **default params** (ask before any non-default env var); **n≥5** per cell; **IC2-5 gate** before defaulting; tune **per-axis**.
<!-- STATUS:END -->

## §0 — What this is & how to use it
This is the orientation layer for PLASMC tuning. At session start the hook gives you the STATUS block; for any real tuning or failure-diagnosis work, **read this whole file**, then pull live data (§2) and open the relevant deep doc (§11). Update §STATUS + the relevant section when a round finishes.

## §1 — The system in one paragraph
PLASMC lands a quadrotor by image-based visual servoing, **scale-free & depth-free** (no altitude/metric in the control law — hard constraint). **Outer loop** (Python PID): normalized pixel error `s_e_n` → desired optic-flow `h_d`. **Middle loop** (Python adaptive SMC): drives measured optic flow `h` to `h_d` through a log-barrier performance funnel with an adaptive gain `κ`. **Yaw** (Python SMC on the image `alpha` feature — compass-free). **Inner loop:** PX4's geometric controller (we ship body-rates + thrust over MAVSDK; PX4 does actuator allocation). NED frame throughout.

## §2 — How to gather the current data (run these to orient)
- **Parameter record (canonical):** `test_data/Landing_Test/parameter_record.ods`. `pandas` is absent — use odfpy:
  ```python
  from odf.opendocument import load; from odf.table import Table,TableRow,TableCell; from odf.text import P
  def cell(c): return " ".join(str(p) for p in c.getElementsByType(P)).strip()
  def row(r):
      o=[]; [o.extend([cell(c)]*int(c.getAttribute("numbercolumnsrepeated") or 1)) for c in r.getElementsByType(TableCell)]; return o
  doc=load("test_data/Landing_Test/parameter_record.ods")
  for t in doc.spreadsheet.getElementsByType(Table):       # sheets:
      print(t.getAttribute("name"))                        #   PX4_NewCal_Record (NC1-49 = current R3 era, the one to read)
      for r in t.getElementsByType(TableRow): print(row(r))#   PX4_Gain_Record (G1-60 = R1/R2 history) · MATLAB_Test_Record · Removed_Parameters
  ```
  Each NC/G row now carries **`Bundle` + `Timestamp`** provenance columns → maps a trial to its `test_data/` data.
- **Test data:** named bundles `test_data/<Name>/<YYYYMMDD-HHMMSS>/rep<N>/` (+ a `summary.tsv` per bundle) AND raw per-landing autosaves `test_data/Landing_Test/<Day Mon D HH-MM-SS YYYY>/`. Each rep dir: `Control_Params.npy` (the gains that ran), `Ground_Truth.npy` (`UAV/Target Pose`, `SoftPrecise`), `Control_Data.npy`, `Img_Data.npy`, `Telemetry_Data.npy`.
- **Memory (auto-loaded index):** `~/.claude/projects/-home-shubham-Soft-Precise-Landing/memory/MEMORY.md`. Key files: `reference_tuning_trajectory` (the whole campaign arc), `reference_docs_folder`, and the `feedback_*` lessons.
- **Code defaults:** `src/controller.py` (the `pa()` / `os.environ.get` block, ~lines 120–175) is the source of truth for baked values.
- **Git history:** tuning milestones are in the commit log (`git log --oneline -- PX4_Gazebo/src/controller.py`).

## §3 — Cal regimes (the master variable)
| Regime | When | Trust |
|---|---|---|
| R0 (broken) | pre-June | ~2000 reps at 2–13× mis-scale. **Confounded.** Only lag timing (38/52–61/287 ms) survives. |
| R1/R2 (multisine) | Jun 2–4 | the "28% SP @10cm" (G46) is a **hypothesis**, not a result. |
| **R3 (honest)** | Jun 5–10 | **the only trustworthy data** (NC1–49). |
A number from one regime does **not** transfer to another — the #1 historical analysis error.

## §4 — Parameter inventory
Knobs are **direct per-axis values** `PLASMC_<PARAM>_{X,Y,Z}` (all `*_SCALE` factors removed 2026-06-03). Current baked defaults are in §STATUS. For the full per-parameter role/empirical analysis, read **`docs/PARAMETER_ANALYSIS.md` §3**; for the complete env-knob table + sweep methodology, the **`tune-plasmc` skill**. Groups: Outer PID + outer funnel (`KP, KI, KD, XIS/gamma_s, PS0, PSINF, DH_D_MAX, TAU_DS`); middle SMC (`XI2, P20, P2INF, OMEGA, GAMMA, E, N, P, KAPPA0, KAPPA_MAX`); yaw (`YAW_*, KR_YAW, PSID_RATE, TAU_UA`); visibility (`FUNNEL_MODE, THETA_FLOOR, RHOFOV*`); inner (`KR_*, W_U_MAX`); image (`IMG_FEATURE_FILTER, MARKER_KLT_MAX_STEPS, ARUCO_*, BODY_YAW_SOURCE, CTRL_ZERO_WXY`); landing-test (`LANDING_REF_RAD_OPT_FLOW, LANDING_IC_*`).

## §5 — Failure modes & the κ-runaway explosion chain
Almost every catastrophe is one chain (detail in `PARAMETER_ANALYSIS.md` §2):
```
lateral drift → outer PID windup (h_d ≈ K_rp·s_e_n/G_s) → loom funnel can't track within p_2inf
   → barrier ζ→∞ → |σ|>E → κ-ODE runaway → a_u blowup → hard impact / fly-away / TARGET_LOST
```
Bounding rules: **P** bounds κ cleanly (`κ_eq∝1/P`); **E** bounds κ but softens tracking (one-knob-one-job: P for κ, E for stiffness); **N, Γ** can't fix it (math-excluded); **cbf2 masks it** (if the CBF bites in normal ops the control law is failing); **funnel width = gain** (never widen a funnel for a transient). The **current binding limit is stochastic LK/ArUco perception** (~2 m/s dynamic-range ceiling → 1–2 TARGET_LOST per 5 reps even at a perfect IC) — *not* gain-tunable. Lag (38 ms roll / 287 ms yaw) is the **last** explanation, not the first.

**θ_norm is contained, not eliminated.** θ_norm (= ‖Θ‖_F, drives `dκ/dt`, ~99% `cross(dw,s)`) still **spikes** (~480 baked, up to 1226 if E_z is narrowed) from a frame-jump `dw` artifact + close-range perception. The κ-cap (`KAPPA_MAX_Z=3`) + P-leakage contain it **downstream** — at the baked config it never even ran κ away. **Do not source-fix it via a `dw` rewrite** (tried 2026-06-10, net-negative: `dw` feeds the load-bearing `c` feedforward → *more* κ-runaways). Detail: `PARAMETER_ANALYSIS.md` §2.

## §6 — Methodology (the rules — violate at your peril)
1. **n ≥ 5** per cell — n=1 is noise (debunked repeatedly). 2. **IC2-5 gate** (`run_ic_validation.sh`, never pass it `LANDING_OUT_BASE`) before changing any default — IC1 wins routinely regress off-center. 3. **Per-axis**, not uniform (image-x is 1.39× hotter than y). 4. **One knob, one job.** 5. **Reject a value on a single TL/crash** (100% reliability needed; a single failure reveals the binding constraint — fix that first). 6. **Clamps are band-aids** — tune the bare law, re-engage protective clamps after. 7. **Sanity-check every SP** against its trajectory (§8). 8. **Use defaults; ask before non-default env vars.** 9. **Don't run SITL** — prepare the command, the user runs it.

## §7 — Known dead-ends & winners (R3)
**Dead-ends (don't retry):** `KI≥2` / `KI=0.35` · `KP≥13` · `KP=12 + E_XY=2.5` · `W_U_MAX>1.7` · `E_Z≥1.5` · `N_XY=0.05` · `N_Z=0.05` alone · `tau_ua=0.3` · `P_XY=3` · `TAU_DS=0.05` · `K_rd=0` alone (needs gamma_s=1.0) · `gamma_s=0.1` · `KR_YAW≠2` · `YAW_OMEGA=1` · `YAW_GAMMA=1` · RHOFOVINF/THETACAP terminal sweeps (frontier mapped) · `MC_*RATE_P>1` via MAVSDK · enlarging the marker · sensor-cal refresh via `aggregate_calibration.py`.
**Winners / baked:** `K_rd=0 + gamma_s=1.0` (the 1.32 m stack) · `KAPPA0_Z=1.0 + KAPPA_MAX_Z=3.0 + κ-freeze` (descent bootstrap) · `P=5/5/5` (κ bound) · yaw 2π-wrap + conditional `ie_a` integration · cbf2 · `BODY_YAW_SOURCE=alpha`.

## §8 — Data-integrity gotchas
- **Cal regime** (§3) — check it before trusting any number.
- **False SPs (frozen GT).** The `SoftPrecise` flag can fire on a degenerate GT: if `UAV Pose` freezes at the IC then resets to the origin, `xy_err`→~1e-21 trips `precise` with no real landing. **There is currently no verified SP in the saved R3 data** (the only sub-10 cm rep was this artifact — `Landing_Test/Wed Jun 10 01-22-38`, marked FALSE). Before trusting an SP: did the drone **descend** (min alt→~0)? Is `xy_err` physical (~0.02–0.06), not ~1e-21? Is the pose non-frozen? See memory `feedback_false_sp_frozen_gt`.
- **Provenance:** the `.ods` carries `Bundle`/`Timestamp`; some Jun-9/10 trials are loose autosaves bundled retroactively (`GammaS_sweep_n25`, `BootstrapFix_n21`, …).

## §9 — Diagnostic tools (`tools/`)
`analyze_explosion_chain.py <rep>` (first-exploding state + causal chain) · `analyze_saturation_audit.py --glob/--events` (limit duty cycle + per-event reason; the standard batch diagnostic) · `analyze_timeseries.py` (per-channel corr with xy_end) · `diagnose_failure_cause.py <rep>` (perception-vs-control at onset) · `analyze_impulse_response.py` (per-axis lag) · `analyze_sigma_compare.py` (MATLAB-vs-PX4 σ).

## §10 — Workflow for a new tuning task
1. Read §STATUS + this guide; pull the latest `.ods` (§2). 2. **Identify the failure mode** (§5) — if it's stochastic perception or lag, a knob won't fix it. 3. Pick **ONE** parameter; sweep **3–5 values** spanning [0.5×,2×], **n≥5** at IC1 (write the command, the user runs `run_knob_sweep.sh`). 4. Analyze (§9); reject on a single failure (§6). 5. If promising at n≥5 → **IC2-5 gate**. 6. **Update** the `.ods` (with provenance), the relevant memory, this §STATUS block, and `PARAMETER_ANALYSIS.md` if a default changes.

## §11 — Deep-dive pointers
- **`tune-plasmc` skill** (`/tune-plasmc`) — the tuning playbook: full env-knob inventory, sweep methodology, the 8 failure modes, known-bad/known-good configs, the diagnostic chain.
- **`docs/PARAMETER_ANALYSIS.md`** — comprehensive per-parameter analysis + the explosion chain + dead-ends + SP-flag integrity (honest-cal rewrite).
- **memory `reference_tuning_trajectory`** — the connected campaign timeline (every trial G#/NC#: what varied, why, outcome).
- **`docs/CONTROLLER_PARITY.md`** (MATLAB↔Python diff + intentional divergences) · **`docs/FUNNEL_CBF_DESIGN.md`** (cbf2) · **`docs/PERCEPTION_FLOW_FINDINGS.md`** (perception layer + LK dynamic-range lever) · **`docs/SH_REFERENCE.md`** (bash patterns).
- **`parameter_record.ods`** — the raw quantitative record (4 sheets).
- **memory `MEMORY.md`** — the full index of `feedback_*`/`project_*` lessons.
