---
name: feedback_flow_funnel_zetah_works
description: "⭐⭐⭐ THE VELOCITY-DAMPING LEVER WORKS + EXTENDED (current frontier). Tightening the lateral flow funnel (XI2_xy 0.2->0.5->0.7) engages zeta_h -> arrests v_lat during descent -> drops the SAFE FLOOR (altitude where the terminal 1/Z loop fires) ~1m->~0.08m. XI2_xy=0.7/P20_xy=15 GT-FB IC1-5 n=2 = 10/10 BOUNDED, ZERO fly-aways (vs 6/2/2 at 0.5). XI2_xy=0.9/P20=10 fixes the high-start IC4 (mean xy 2.84->1.04). REFRAME (2026-06-26): the reported xy is NOT a precision failure — the controller touches down DEAD-CENTERED (5/10 reps lat 0.005-0.09m at min-Z) then a terminal 1/Z kick balloons it 1-4m; reported xy = post-kick endpoint. The genuine residual is SOFT = terminal velocity (vlat 0.5-1.5/vz 0-1.3 m/s ~= 38ms lag, GT-FB ceiling xy~0.2/relvel~0.37). s_e_n CONVERGES (min 0.014-0.05, 86-97% in p_r, breach terminal sub-15cm); lateral h_e is DEGENERATE (h_d=meas s_dot -> h_e~0 by construction) so only s_e_n + Z-channel h_e_z are informative. h_e_z rides its funnel at a fixed ~0.6 ratio; the combined auto-align had LOOSENED p_z (P2INF_z 0.5->1.5, XI2_z 0.6->0.2); tightening it scales |h_e_z| 1.3->0.35 but ratio persists + adds mild bounded z-chatter."
metadata:
  node_type: memory
  type: feedback
  originSessionId: 31fd53ca-48b0-48f4-81a8-2e081955028f
---

## 2026-06-26 — LEVER EXTENDED (XI2_xy 0.7/0.9) + the residual RE-FRAMED to SOFT (user-led GT-FB)
**Extended the lever and re-framed what's left. The lateral POSITION problem is solved; the remaining gap is terminal VELOCITY (soft), not precision.**

- **`XI2_xy=0.7` + `P20_xy=15` (GT-FB IC1-5, n=2) = 10/10 BOUNDED, ZERO fly-aways** (vs the `XI2=0.5` 6 sub/2 marg/2 fly). Terminal velocity crushed (mean vel 0.6-1.5 vs 1.8-2.9). The safe-floor dropped ~0.15m -> ~0.08m (the 1/Z loop now fires *below* touchdown contact on most reps). Bundle `20260625-213624`.
- **`XI2_xy=0.9` + `P20_xy=10` fixes the high-start IC4** (the one cell still carrying unarrested v_lat: lat 0.90/vlat 0.35 @ Z=1m at XI2=0.7). IC4 n=3: mean xy **2.84->1.04**, max 4.16->2.28, rep1/rep3 0.34/0.49. Faster funnel contraction arrests the higher-start lateral. But rel_vel stays 1.4-1.5 (NOT soft) -> position lever can't touch terminal velocity.
- **IC2_rep1 trace (the former 1.87 fly):** NOT a v_lat-arrest failure (handoff was right). v_lat arrested to Z=0.11 (lat 0.005-0.02). The runaway is a terminal **tilt->GT-flow positive-feedback** at Z<0.1m: kappa_x was ASLEEP (h_x~0 through the clean descent) -> a small deck-level x-excitation woke it too late (N=0.1) -> theta detonates 2deg->40deg->396deg, a_u_xy->2525. Tightening XI2 makes the breach *more* violent (a_u 1845->2525) but fire LATER/lower -> net bounded. GT-FB so this is delivered tilt creating real flow, not an FF artifact.
- **⭐ PRECISION-METRIC REFRAME (load-bearing):** the controller TOUCHES DOWN DEAD-CENTERED. At min-Z, **5/10 reps are sub-decimeter (lat 0.005-0.089m)** — including all 3 the summary scores as 1.7-4.1m "flys" (IC1_rep1 0.005, IC2_rep1 0.022, IC4_rep1 0.032). The terminal 1/Z kick fires AT contact and balloons the (already-landed) drone 1-4m up+sideways (vz flips to +2.8/+3.1/+4.0 = climbing). **The reported `xy_err` is the post-kick balloon endpoint, NOT the precision achieved.** So the "1-2m precision plateau" was a metric artifact; the genuine residual is the ~1 m/s terminal LATERAL velocity (soft), which then feeds the kick.
- **Convergence/boundedness (s_e_n + h_e):** s_e_n CONVERGES 9/10 (min 0.014-0.05), 86-97% inside p_r, breach is terminal + sub-15cm (Z 0.07-0.22m) = the safe-floor. **Lateral `h_e` is DEGENERATE** (combined `h_d`=measured s_dot -> `h_e`=h-h_d~0 by construction; 100% "inside p_2" carries NO info — do not read it as soft progress). Only `s_e_n` and the Z-channel `h_e_z` are informative barriers.
- **Z funnel (user's hypothesis = correct):** `h_e_z` never converges — it rides its funnel at a fixed ~0.6 ratio (|h_e_z|_term/p_z 0.14-0.78), worst on the worst reps. ROOT: the combined auto-align (controller.py L268/L283) RESET the z funnel to VDF (`P2INF_z` 0.5->1.5, `XI2_z` 0.6->0.2) = 3x looser floor + 3x slower contraction vs the old soft-config, silently un-doing the soft-touchdown z-tightening. Tightening back (`XI2_z=0.6`, `P2INF_z=0.5`) scales `|h_e_z|` 1.3->0.35 (funnel floor 1.9->0.50 as commanded) BUT the ratio persists (saturated-barrier: SMC holds h_e_z in the boundary layer at ~0.6*p_z) AND adds mild bounded z-chatter (sigma sign-flips up to 17, kappa_z bounded 0.9-2.0, no fly). **vz-softening verdict pending IC4/IC5** (the high-vz cells). s_e_n convergence UNHARMED by the z-tightening.
- **⚠ The old caution "full soft-config-Z on combined = 5/5 fly catastrophic" is STALE** (measured pre-velocity-damping, pre-E_z=0.5, over-hot base). Do NOT cite as binding; judge z-changes fresh by the limit-cycle + fly metrics.

**NEXT (open):** (1) finish the z-funnel A/B on IC4/IC5 -> does tighter p_z kill the *vertical* terminal component. (2) the lateral terminal kick magnitude (theta detonation) is the irreducible Z->0 residual at the deck — likely needs a touchdown-disarm / commit gate, NOT more funnel. (3) perception-ON validation (drop PLASMC_GT_FEEDBACK) before any bake. h_rd CONSTANT throughout.

---

**[ORIGIN] THE VELOCITY-DAMPING LEVER IS CONFIRMED — the fix the whole terminal thread pointed to (2026-06-25, user-led GT-FB).**

**The lever.** sigma_xy = zeta_h + chi_r*zeta_r. zeta_h (the flow/velocity barrier) was DORMANT (~7% of sigma_xy) because the lateral flow funnel was too loose (p2_xy stayed 6-9 during the descent; XI2_xy=0.2 contracts too slowly). **Tighten it: XI2_xy 0.2->0.5** (env `PLASMC_XI2_X=0.5 PLASMC_XI2_Y=0.5 PLASMC_XI2_Z=0.2`; keep Z=0.2 = VDF, else the single-axis env BYPASSES the auto-align and reverts XI2_z hot). Result: p2_xy contracts to ~2-4, **zeta_h share 7%->18%** -> the SMC reaching term now drives v_lat->0 *during* the descent (while Z is large and the disturbance is under the 17 m/s^2 actuator ceiling).

**Result: GT-FB tally 6 sub / 2 marg / 2 fly** (bundle 20260625-202813) vs the same-config baseline **175559 (XI2_xy=0.2): 4/2/3**. A clean win at the SAME N_xy=0.1, chi_r=0.5, P2INF=[0.5,0.5,1.5].

**Mechanism = the SAFE FLOOR drops.** The terminal fly is the 1/Z feedback loop: h_xy=v_lat/Z -> control over-reacts -> tilt -> lateral accel -> v_lat grows -> bigger h_xy. The loop GAIN is ~1/Z, so it always wins eventually as Z->0. zeta_h damping doesn't remove the loop — it **lowers the altitude where the loop overpowers the damping** from ~1m (175559: v_lat survived to 1m -> breach@1m -> fly) to **~0.15m** (most reps now touch down BEFORE the loop triggers -> sub). vlat@1m: subs 0.02-0.73 (the SP condition <0.5), the fly IC4_rep1 = 1.0 (not arrested).

**h_xy is NOT eliminated and CAN'T be (irreducible geometric 1/Z).** h_xy=v_lat/Z spikes as Z->0 for ANY non-zero v_lat. On SUBS the "spike" (hxy_deck up to 50) is the TOUCHDOWN sample (Z<0.1, drone landed, GT bounces) — h_xy is <=2.5 through the real descent = benign. On FLYS v_lat RE-GROWS in the last ~15cm (0.34->2.29 as Z 0.15->0.10) -> real spike (174). **The right metric is the SAFE-FLOOR ALTITUDE, NOT the hxy_deck magnitude.** (On flys h_xy=174 >> v_lat/Z=24 because the violent terminal tilt adds ROTATIONAL flow on top of v_lat/Z.)

**Side benefits (clean lever).** (1) **NO limit cycle introduced** — all 4 axes sat(sigma/E)<=1.48, velocity sign-flips 1-3 (not a sustained oscillation). (2) **The LOOM cycle is SUPPRESSED** even at the baked E_z=0.5 (the setting that earlier gave sat_z 5-8 bang-bang balloon): here sat_z=1.48, no balloon — arresting v_lat cuts the lateral->tilt->loom coupling that fed the cycle. (3) **kappa healthy in all 4 axes** on the subs: x/y ADAPT (std 0.23, the N_xy=0.1 wake working) with benign terminal growth (3.8, NOT the fly-only 13-46 explosion); z low (0.095, term 0.55); yaw decays (0.11->0.10). The kappa explosion is FLY-ONLY (needs the v_lat re-growth loop to trigger).

**NEXT STEPS (NOT baked — iterate then validate).**
1. **Tighten more: XI2_xy 0.5->0.7** (and/or lower p_2_0_xy from 25) -> push zeta_h share past 30% and the safe-floor below ~0.05m -> should catch IC4_rep1 (the v_lat-not-arrested fly).
2. **Trace IC2_rep1** (flew 1.87 with v_lat ARRESTED to Z=0.11 then a SUDDEN late spike v_lat 0.04->2.92) — NOT a v_lat-arrest failure; smells like a woken-kappa (N=0.1) terminal transient. Different mechanism.
3. After a clean XI2_xy gate: validate n>=5, then perception-ON (not GT-FB), THEN consider baking XI2_xy.
4. h_rd CONSTANT throughout (the vz=h_rd*Z taper IS the soft-landing mechanism — never reference-govern it).

Continues [[feedback_terminal_smc_actuator_wall]] (the "arrest v_lat early" prescription — now VERIFIED), [[feedback_kappa_4axis_hexy_param_map]] (the velocity-damping lever was the predicted fix), [[feedback_matlab_gains_not_portable]] (the lag is why we can't just copy MATLAB's chi_r). Reproduce: `HEADLESS=1 PLASMC_GT_FEEDBACK=1 PLASMC_XI2_X=0.5 PLASMC_XI2_Y=0.5 PLASMC_XI2_Z=0.2 N_REPS=2 bash scripts/run_ic_validation.sh`.
