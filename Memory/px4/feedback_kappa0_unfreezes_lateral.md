---
name: feedback_kappa0_unfreezes_lateral
description: "⭐⭐ kappa_0_xy raised 0.125->0.5 UN-FREEZES the frozen lateral kappa and converges s_e_n -> fixes the lateral residual (GT-FB IC4 2/2 SP, IC1 2/2 SP, n=2). The lateral IS gain-tunable -- OVERTURNS the 'not gain-tunable / perception / architecture / inner-loop-velocity / LK-dynamic-range / lag' framing. Fix is kappa_0 (PERSISTS via slow tau_xy=6.7s), NOT N_xy. Chatter = degenerate flow barrier zeta_h breaking into sigma=zeta_h+chi_r*zeta_r, MASKED by chi_r*zeta_r; every TIGHTENING lever (E/Gamma/XI2/floor) ejects sigma from the boundary layer -> chatter; kappa_0 + chi_r are the only SMOOTH levers. Terminal 1/Z is a FINITE rate race (lat must converge faster than |h_rd|=0.42/s), NOT an unregulatable singularity. STALE: chi_r=2.0 / XI2_z=1.0 'catastrophic' were the OLD frozen-kappa base."
metadata: 
  node_type: memory
  type: feedback
  originSessionId: f1723146-d87f-40d0-9a34-49233fbc4e72
---

## 2026-06-26 — kappa_0_xy un-freezes the lateral (GT-FB, user-led). The lateral IS gain-tunable.

The frozen lateral kappa_xy is the bottleneck behind the "lateral residual / terminal kick", and it is **fixable with a gain** — overturning the long-standing "not gain-tunable / perception / architecture / inner-loop-velocity / LK-dynamic-range / 38ms-lag" framing across the old memory.

### The fix: kappa_0_xy 0.125 -> 0.5 (NOT N_xy)
- **kappa_0_xy=0.5 HELD ~0.40 through the descent** (vs frozen ~0.15) -> s_e_n converged deeper (0.66->0.41 at 1m, 0.43->0.18 at 0.5m) -> **GT-FB IC4 2/2 SP (0.034, 0.075), IC1 2/2 SP (0.055, 0.053)**, no over-command. kappa_0_xy=1.0 also clean (held ~0.80, sigma 4 flips, 2/2 SP) -> the smooth gain SCALES, no lag-ceiling hit yet.
- **Why kappa_0 and not N_xy:** tau_xy = 1/(N*P) = 1/(0.1*1.5) = **6.7s ~ the descent**, so kappa can't RAMP in time (the "N_xy=0.1 wakes kappa" lever regresses — the woken kappa amplifies the terminal). BUT the same slow tau makes a raised kappa_0 **PERSIST** (decays 0.5->0.40 only). So the lever is the BOOTSTRAP gain (start un-frozen), not the adapt rate. Contrast z: tau_z=2s (P_z=5) -> kappa_0_z LEAKS (1.0->0.04), so kappa_0_z is NOT the z-lever (XI2_z funnel is). See [[feedback_kappa_4axis_hexy_param_map]] (its "N_xy is the lever" framing is now refined to "kappa_0").

### Chatter = the degenerate flow barrier zeta_h, MASKED by chi_r*zeta_r
Code-confirmed sigma_xy = zeta_h + chi_r*zeta_r (controller.py:1155-1156). Decomposed on the data:
- **s_e_n is SMOOTH (1 zero-crossing) in EVERY config** — it does NOT oscillate. "Chatter = s_e_n not converging" is WRONG as stated.
- The chatter (sigma sign-flips) is the **degenerate flow barrier zeta_h** (h_e~0 by common-mode cancellation, but NOISY around 0; flips 14-20x in all configs) breaking into sigma. It's a feedback loop: aggressive control -> sat bang-bang -> excites optical flow -> zeta_h inflates (0.06->0.16) -> flips sigma -> more control chatter.
- **chi_r*zeta_r (the s_e_n barrier) is the MASK:** sigma flips wherever |zeta_h| ~ |chi_r*zeta_r| (they cancel). A strong, well-converged s_e_n barrier dominates sigma and HIDES the flow noise. So s_e_n's ROLE is the stabilizer, not the oscillator.
- **Every TIGHTENING lever ejects sigma from the boundary layer -> chatter:** E_xy=0.5 (22 flips, sat->1 bang-bang), Gamma_xy=1.0 (24/26 flips + overshoot, high variance 0.01/0.30), XI2_xy=1.0 (20 flips, p_2 denominator->0 so even h_e~0 breaches), p_2_inf_z=0.5 (regresses, h_e_z 0.42->0.55). ALL confirmed dead-ends on the kappa_0 base — for the SAME mechanism (chatter), now understood.
- **The only SMOOTH authority levers: kappa_0 (gain multiplier on sat, scales without switching) and chi_r (raises the position-barrier mask).** chi_r-up should BREAK the chatter loop (mask sigma -> sat stops flipping -> control smooth -> flow not excited). Structural alternative: drop the degenerate zeta_h from sigma entirely (sigma=chi_r*zeta_r). Both UNTESTED-but-queued as of this writing.

### Terminal 1/Z is a FINITE rate race, NOT an unregulatable singularity
Derived: s_en = lat/(Z*p_10), and with Z_dot/Z = h_rd = -0.42, **ds_en/dt = (1/(p_10*Z))*[lat_dot + 0.42*lat]**. So s_e_n converges IFF **lat converges faster than |h_rd| = 0.42/s** (a finite, achievable rate). The 1/Z factor only sets MAGNITUDE; the SIGN is the bracket. So:
- The terminal blow-up is NOT a physical singularity — it is the s_e_n-NORMALIZED control law OVER-REACTING (demands a_u 660, 40x the ~17 m/s^2 deliverable -> actuator saturates -> can't deliver -> lat GROWS -> runaway) on an UN-converged residual (lat plateaus at the lag floor, lat_dot->0 -> bracket -> +0.42*lat > 0).
- **Regulatable in principle:** converge lat faster (kappa_0 raises k_lat) or reduce the 38ms lag. The gate (commit before the deck) handles only the IRREDUCIBLE lag-floor tail. This REFINES [[feedback_terminal_smc_actuator_wall]] / project_gt_feedback_control_tuning ("kappa cannot chase unbounded 1/Z" / Lyapunov "fundamental") -> it's a control-law over-reaction + actuator saturation, not an unregulatable physics. The gate is the principled tail-handler, not a band-aid.

### STALE "catastrophic" anchors (were the OLD frozen-kappa base — re-test on the kappa_0 base)
- **chi_r=2.0 "catastrophic" ([[feedback_matlab_gains_not_portable]]):** measured on the frozen-kappa base where s_e_n never converged -> chi_r weighted a large un-shrinking zeta_r -> infeasible demand. On the kappa_0 base (s_e_n converges) the ceiling is likely MUCH higher; the chatter analysis predicts higher chi_r = stronger mask = LESS chatter. Do NOT cite 2.0 as binding; find the real ceiling empirically.
- **XI2_z=1.0 "catastrophic bang-bang fly":** was the frozen-kappa base (z-chatter coupling into the un-converged lateral via the shared scalar theta). On the kappa_0 base it is 2/2 SP and marginally BETTER than 0.6 (max|h_e_z| 0.42-1.12 vs 1.22-1.38; chatter comparable 13-23 flips). **BAKED provisional** XI2_z 0.8->1.0 in controller.py:184 bare default (2026-06-26; bare-default-only to avoid the no-env Frankenstein the prior auto-align bake hit). The performance function (funnel) is the z-lever: XI2_z = the prescribed CONVERGENCE RATE of h_e_z; faster -> forces vz->h_rd*Z tracking earlier -> lower h_e_z. Ceiling = the control's tracking authority (funnel can't outrun it).

### s_e_n DIVERGENCE = a lateral OVERSHOOT (under-damped loop); CAUSALITY is divergence->chatter
- **s_e_n converges to a min (~3m) then DIVERGES to the deck — UNIVERSAL.** Scan of 737 genuine reps: NO config converges DEEP (sen_min<0.1) AND stays; the only div<1.5 reps NEVER converged (sen_min 0.3-1.0, plateaued high = failures). Smallest divergence at deep convergence = kappa_0=0.5 dead-centered (sen_min 0.04 -> term ~0.15, div 4x). "Keep s_e_n converged to touchdown" is an OPEN problem.
- **The divergence is NOT a plateaued lat amplified by 1/Z — `lat` ACTUALLY GROWS (a lateral OVERSHOOT):** kappa_0=0.5 lat 0.12->0.45 (peak ~1.5m); kappa_0=1.0 lat 0.005->1.20. More authority (kappa_0=1.0) converges DEEPER (sen_min 0.005) but overshoots HARDER = **UNDER-DAMPED lateral loop** (loop gain prop 1/Z RISES during descent -> crosses under-damped ~2-3m -> overshoot, amplified by s_e_n=lat/Z).
- **CAUSALITY (user-corrected): the s_e_n divergence is the ROOT (geometric); the sigma-CHATTER is the control's REACTION to it (downstream symptom), NOT the cause.** (My earlier "chatter -> plateau -> divergence" was backwards; the plateau/overshoot is set by under-damping + the lag floor, not the chatter.) Fix the divergence -> the chatter dissolves.
- **FIX = DAMPING that scales with the 1/Z gain, NOT more authority.** zeta_h (velocity damping) is degenerate (can't damp). `chi_r` carries a position-RATE term `chi_r*dzeta_r` in the control (chi_zeta_aug, line 1160 = a D-term) -> raising chi_r adds DAMPING. So chi_r is the lever for 3 reasons: convergence (sigma weight) + chatter-mask + overshoot-DAMPING. chi_r 0.5->0.85 INCONCLUSIVE at n=2 (step too small + XI2_z=1.0-vs-0.6 confound; sigma flips comparable 2-7 vs 4-5, 1 soft-miss); the real test = chi_r=1.5 on a MATCHED base + proper post-min overshoot metric, n>=3.

### χ_r=1.5 = the lateral FRONTIER; over-aggression = the lag-BANDWIDTH ceiling (2026-06-26 cont., user-led)
- **χ_r=1.5 is the lateral optimum.** `χ_r·dζ_r` is the velocity-damping **D-term** (the combined-surface σ̇ term); χ_r 0.5->1.5 KILLS the hump (overshoot 0.45->0.05) + best precision (IC4 2/3 SP, IC1 2/2 @ 0.011-0.016). χ_r=2.0 OVER-aggresses (⚠ audit 2026-07-02: only n=2 on disk, 0/2 non-SP; recorded a_u_xy peaks 6103/10324 at the touchdown instant, pre-touchdown p99 492/759 — direction holds, the "3000-4600, 0/3" doesn't match surviving data). χ_r is a BALANCED PD knob (raises position weight `χ_r·ζ_r` AND damping `χ_r·dζ_r` together) — cleanest lateral lever.
- **The hump = under-damped overshoot** because ζ_h (the flow/VELOCITY barrier) is DEGENERATE (h_e≈0) -> NO velocity feedback -> position-only loop on a lagged double-integrator -> overshoots. χ_r·dζ_r restores the damping via the POSITION-rate channel (the flow channel is dead). The c-term loom×flow `-(h·e3)h` AMPLIFIES the unregulated v_lat (1/Z) but is a SYMPTOM, not the root (the controller can't prevent the outward motion because it lacks velocity feedback, not because the c-term is rogue). The architecturally-correct fix = revive ζ_h (bounded reference-h_d / backstepping); χ_r is the working near-term fix.
- **OVER-AGGRESSION = exceeding the lag-limited BANDWIDTH.** ANY lateral gain past the χ_r=1.5 frontier (Γ_xy=1.0, E_xy=0.5, XI2_xy=1.0, χ_r=2.0) -> hump RETURNS (0/3). NOT a P/D imbalance (loop stays damping-dominated, P/D~0.3). The aggression raises the loop gain/BANDWIDTH; the FIXED 38ms inner lag then eats the phase margin -> the (lagged) damping arrives too late -> overshoot. The 38ms lag is the UNIFIED ceiling (soft-residual floor + κ-rate limit + THIS bandwidth limit). "Can't out-gain the 38ms lag" — only uXRCE-DDS lifts it. Frontier sits AT the bandwidth optimum.
- **The "chatter-failed" params are CORRECTLY DEAD — by OVER-AGGRESSION, not chatter.** E_xy/Γ_xy/XI2_xy on χ_r=1.5 over-aggress (hump returns) while CLEAN (1-4 σ_x flips; Γ fails CATASTROPHICALLY at 1-2 flips). Robust to XI2_z (tested @0.6 AND 1.0). ⚠ MY-ERROR CORRECTION: "XI2_xy=1.0 chatters 20 flips, dead" was a BUNDLE CONFUSION — the 20 flips were the ADJACENT XI2_z=1.0 run; XI2_xy=1.0 ALONE is clean (1-2 flips, 2/2 SP @ χ_r=0.5). (E_xy's "22 flips" @χ_r=0.5 was REAL.)
- **XI2_z lateral-chatter confound: real at χ_r=0.5** (XI2_z=1.0 -> 20 lateral σ_x flips via z↔lateral coupling) **but MASKED at χ_r=1.5** (χ_r·ζ_r dominates σ -> 1-11 flips, noise). So XI2_z=1.0 bake JUSTIFIED at χ_r=1.5 (lower h_e_z, masked chatter); cost only bites at χ_r=0.5.
- **κ_0_z is INERT for the soft.** τ_z=1/(N_z·P_z)=2s << ~15s descent -> κ_0_z is an INITIAL CONDITION washed out before the terminal (0.25 and 1.0 both leak to ~0.05). Sweeping κ_0_z is meaningless; the soft residual is the 38ms LAG FLOOR. κ_z ADAPTS (NOT frozen like κ_xy — confirmed across runs: leaks low, ramps to ~0.35). No z-analog of the κ_0_xy fix.
- **CONTROL ROUGHNESS: every control derivative is ALREADY filtered.** h (smooth4+GT_FB_TAU/savgol/KF), ḣ_d (CV-KF DHD_KF baked), **dζ_r (CV-KF via VDS-KF: dr_bar_e=s_dot_meas/p_10, s_dot_meas=vdsKFStep, VDS_KF baked default-on)**, ds_e_n/dζ_s (smooth4, legacy path), dp (analytic). NOTHING unfiltered — an earlier "dζ_r roughness 0.43" was a RAW np.gradient(ζ_r) reconstruction ARTIFACT. Prefer CV-KF for runtime derivs (lag-optimal); savgol offline only. Smoothness levers IF needed: K_R relax (raised 1.5->2.5 for the OLD under-damping χ_r now fixes -> likely SUPERSEDED), χ_r=1.2, c-term spike caps — NOT new filters.
- **CORRECTED CAUSALITY: divergence -> chatter** (NOT chatter -> divergence). The hump (lateral drift-out) = lateral MOTION -> excites optical flow -> inflates the degenerate ζ_h 6-10x -> breaks into σ -> chatter. Chatter is DOWNSTREAM; kill the hump (χ_r) -> chatter vanishes (hump-killed reps 2-3 flips).
- **METHODOLOGY LESSONS (user-enforced):** pin ALL 3 axes (κ_0_z=0.25 & P20_z=10 were silent env-trap Frankensteins); isolation INVALID for COUPLED axes (z↔lateral via shared θ + 1/Z); VERIFY bundle identity before comparing (the 20-flips confusion); recompute metrics on ONE consistent definition; κ_0_z comparison was "wrong" because κ_0_z is washed-out-inert (initial-condition, not terminal).
- **STATUS: IC1-5 n=5 gate on the coherent frontier RUNNING** (XI2=[.7,.7,1.0] κ0=[.5,.5,.25] χ_r=1.5 P20=[15,15,10] E=[1,1,.5] Γ=[.4375,.5,.75] + loom gate). PROVISIONAL until it lands.

### Validation status (DO NOT overclaim)
All GT-FB, mostly IC4 n=2 (the failure provoker; IC1 checked for no-regression). PROVISIONAL — pending IC2-5 n>=5 + perception-ON (where the W_U_MAX/theta caps return via LK). The MECHANISMS (kappa-ODE tau, chatter decomposition, rate-race derivation) are solid; the precise baked VALUES (kappa_0_xy=0.5, XI2_z=1.0, chi_r sweep) need the full gate. Methodology: focused IC4 n=2 A/B + read-by-mechanism (kappa trajectory, s_e_n depth, sigma flips), not just SP rate. Links: [[feedback_combined_surface_divergence]], [[project_current_state]], [[feedback_flow_funnel_zetah_works]].
