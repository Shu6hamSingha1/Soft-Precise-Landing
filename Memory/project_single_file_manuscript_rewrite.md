---
name: single-file-manuscript-rewrite
description: "2026-06-10 session state: complete single-file manuscript draft built (no \\input; supplement folded in); pending para-by-para review starting SII; terminology epochs locked; figure-regen queue; open virtual-compass question"
metadata: 
  node_type: memory
  type: project
  originSessionId: cee6e71e-7634-484c-b772-85dc1fc380cb
---

**State (2026-06-10, commit `1c82c45`):** `manuscript.tex` is now the COMPLETE single-file paper (~960 lines): Abstract → §I (fully validated, every claim source-checked) → Table I (rewritten from scratch + revalidated) → §II Preliminaries → §III VDF-ASMC Design + Stability → §IV Results → §V Conclusion. No `\input{}`. `control_formulation.tex`, `results.tex`, `supplemental.tex` remain on disk as historical sources but are NOT compiled. Backup: `Drafts/manuscript_v8.tex`. Title (user-set): "Scale-Free Adaptive Sliding-Mode Control for Guaranteed Soft-Precise Landing under Target Visibility Constraints".

**Next task (user-stated):** para-by-para review of the new draft, starting with §II (carries the most newly folded supplement material). Draft is intentionally over page budget — trimming happens during review.

**2026-06-10 (later session) — §I closing block fully reworked (uncommitted):** contributions reduced 5→4. Final state: lead-in split into two sentences (proposal || sensor suite), IMU + UAV expanded on first use. (1) merged stability+adaptation: "coupled and nonlinear landing dynamics", "continuous and chattering-free", "unknown bounded uncertainties", tail "where the coupling and disturbances are most pronounced"; "on an unpredictable moving vehicle" deduped (lead-in only). (2) ex-C3 split into 3 sentences: "Time-varying performance constraints" (antecedent fix) + "Lyapunov-stable" hyphen + "transformed optical-flow error" (no "global") + "soft-touchdown functional requirement" (never "guaranteed soft-precise touchdown") + faults/saturation as own sentence. (3) ex-C4: "sized by the margin of" (not "triggered by"); "independently of the transient image parameter errors" (not "outer-loop transient" — no §I antecedent). (4) ex-C5: no experiment counts; "variations in the UAV's initial state, the target state, and the target dynamics"; quotes mean t_f ≤ 18.2 s + horizontal error within **5.8** cm (table max is 5.71 — "5.7" was strictly false; ABSTRACT still says 5.7, fix pending). Roadmap rewritten with \ref{} labels + §II described as quadrotor dynamics/image parameters/problem formulation. Both `---` pairs removed (para 6, §II.B opening); manuscript em-dash-free. check_refs.py green.

**2026-06-14 session (commit 5f6dc01) — CONTROL-FORMULATION REWRITE encoded in §III.** The CBF+SEN port (see [[project_cbf_sen_matlab_port]]) is now reflected in the manuscript. Pre-rewrite snapshot Drafts/manuscript_v10.tex. Changes:
- §III.A.1 PID → **image feature funnel** (back-mapped PPC on normalized image-position error r̄_e: p_s, S_s, ζ_s, G_s, PID-on-ζ; h_d map UNCHANGED). §III.A.3 cone clamp → **target-visibility CBF** (camera-frame barrier h=φ_max−|ᶜr̂|, exact depth-free L_ω·M, CBF-QP on commanded tilt + class-K + θ_cap cap, direct th_safe→R_d, thrust via actual R33). Property 1 → deliverable lateral accel.
- **NEW Theorem 3 (Target-Visibility Forward Invariance)** — user-directed ("add the missing theory"). CLEAN continuous-time CBF certificate on the EXACT model (comparison lemma/Nagumo) + explicit sufficient feasibility condition + perception-floor boundary. User ruled (2026-06-14): implementation patch-ups (15px inset, one-step sampled prediction, O(‖Δθ‖²) linearization residual, attitude-tracking residual) are NOT in the theorem — they go to the impl appendix (manuscript≠replica). Honest framing: visibility is a feasibility-CONDITIONED forward-invariance certificate, NOT an unconditional guarantee (I validated the claim first; the old cone-clamp "kinematic visibility" was itself an overclaim per FUNNEL_CBF_DESIGN §1).
- Corollary 1 → image-feature funnel precision. Remark 7 + summary → Theorems 1–3 + Cor 1. Abstract S6 + §I contribution → image feature funnel + CBF (feasibility-conditioned, no unconditional "guarantee"). Block-diagram caption, gains table (N_z 0.05→0.02; SEN params Ξ_s/p_s0=1.2/p_s∞=0.35; CBF params φ_max-inset/τ=0.3/θ_cap replacing cone-clamp rows), tuning guidance updated.
- **NAMING (user 2026-06-14):** keep VDF-ASMC; two funnels = **image feature funnel** + **optical-flow funnel**; CBF = visibility layer on top. (NOTE: "image feature funnel" OVERRIDES the old feedback_funnel_naming ban — that ban was for the OLD p_1 visibility envelope; architecture changed.)
- **§IV [Pending re-validation] BANNER added** (top of §IV): 25/25, 5.71cm, ±40%, comparison are PRE-CBF/SEN results, being re-run. Exact MATLAB validation so far (single-run): noiseless 5/5, noisy 16/20, IC5 fails (SEN demand-starvation, → Ubuntu); comparison still LOCKED on old.
- **REMAINING (next manuscript pass):** (1) [DONE commit bf804af] impl appendix (Appendix D) rewritten to the cbf2 realization (new cbf_projection eq: alternating projection over corners, τ=0.3 look-ahead, 15px inset=δ_m≥ē Theorem-3 margin, magnitude-clamp fallback; LPF upstream of barrier; r_d3 from θ* direct; Fix-B T_u=m|a_z|/R33). Theorem-1-proof eq-range reordered lpf--floor. Uncertainty appendix actuator-feasibility now cites CBF cap not cone clamp. (2) §IV/App-G stale prose under banner: mechanism-contrast table (says "funnel-margin cone clamp"), IC₂ thrust caption ("cone-induced bound of Property 1"), speed-envelope summary ("θ_cap binding"), accel-floor appendix prose ("cone-clamped command", "θ_cone lateral cap"). (3) block_diagram_v3.pdf figure regen (shows cone clamp + Image Point PID) — needs Ubuntu. (4) re-run §IV under CBF+SEN once IC5/SEN fixed on Ubuntu, refresh all .mat numbers, remove banner.

**2026-06-12 session (commit 70c4860, pushed via merge b4f5144) — abstract + §III content pass:**
- **Abstract v2 APPLIED** (item 1 closed). User revised the draft live: two-deficit gap (cut the "visibility carries no formal guarantee" deficit — it was an overclaim vs Lin2023, which guarantees visibility by design). Final = user draft + 3 fixes I recommended and user accepted: "target's state and motion" (restored target-dynamics dimension), "the target remains visible" (not "target visibility" — feedback_visibility_phrasing), double-space + "state of either the quadrotor or the target". VDF-ASMC acronym introduced. "scale-free" adjective dropped from abstract (title still carries it).
- **virtual-compass RULING CLOSED (item 2): KEEP as named exception** (user, 2026-06-12). Not an image parameter; magnetometer-free yaw-integrator metaphor. No tex change. Removes open-question #1 below.
- **§III content pass DONE.** Validated against corrected kinematics + proofs claim-by-claim: h_d eq (270) consistent with transport ṡ; c_h eq (279) = corrected terms − ḣ_d; d_h eq (212) = corrected form (no Coriolis/abs-velocity); **Corollary 1 proof HOLDS under corrected ṡ** — coupling terms cancel when h=h_d, residual δ_e = h_e−(h_e·ê₃)s linear in h_e & FoV-bounded; unit audit r̄_e=r̂_e⊘(R/2) px/px dimensionless OK; Thm 1 UUB algebra ‖σ‖²≤2Θ→θ matches (378); Thm 2 OK; Remark 6 de-rotation matches corrected model.
- **FIXED:** 4× compile-breaking `\ref{implementation appendix: section}`→`ef{...}` (backslash-r heredoc mangling) at proof + 2 §IV spots + gains table; block-diagram caption "desired optical flow h_d"→"desired image velocity h_d" (epoch-5). check_refs.py: missing targets NONE.
- **TWO content recommendations PENDING USER (drafts-before-apply, NOT applied):** (A) Theorem 1 statement says "drives ζ_2 into UUB θ" but θ is strictly the ‖σ‖ bound; Thm 2 handles the analogous step cleanly ("drives σ_α into ϑ_α; consequently α_e UUB by affine fn"). Recommend aligning Thm 1: "drives σ into UUB θ; consequently ζ_2, hence h_e, stay within the optical-flow funnel." (B) Assumption 2 titled "(perfect attitude tracking)" but body admits a nonzero exp-decaying residual absorbed into d_h — mild oxymoron; recommend "(time-scale separation)"/"(fast attitude tracking)". Item (iii)+Asm 2 both route attitude residual→d_h: consistent (one-time inflation), not double-counted.
- **A/B APPLIED** (commit 21c6350): Thm 1 statement now "drives σ into UUB θ; consequently ζ_2, hence h_e, remain within p_2(t)" (θ bounds ‖σ‖, matches proof V≥‖σ‖²/2 + parallels Thm 2); Assumption 2 retitled "(perfect attitude tracking)"→"(time-scale separation)". Also abstract verb-dedup "which is shown to be the key"→"the key". Abstract IMU clause KEPT (user tried dropping "using only a monocular camera and IMU"; restored — IMU is load-bearing: ψ̇_b feedback-lin + SO(3) attitude/body-rates; dropping it makes "without heading measurement / without the state of the quadrotor" overclaim vs the IMU-based inner loop).
- **§IV review DONE (commit 0f2d7c6).** SAFE FIXES applied: worst-case error "5.7 cm"→"5.71 cm" ×2 (results prose + summary item 1; 5.7 understated table max 5.71 Case 2, disagreed w/ conclusion; abstract/§I keep rounded "within 5.8 cm" bound); "realisations"→"realizations" (line 538, US spelling). Verified consistent: comparison-table Proposed cells (r_xy≤2.1cm, r_z=0.200=h_lg), 33-axis/3325-run sweep arithmetic, 0/80 vs 20/20 velocity-sweep counts, deep-sweep role classification.
- **⚠️ OPEN — needs .mat (I cannot run MATLAB):** Zhang2026 soft-bound SCOPE inconsistency. Line 746 (observations): Zhang precise-fails all 4 moving cases, soft-fails **Case 5 ONLY** (v_f=0.65). Line 800 (per-baseline): "terminal relative speed 0.56–0.66 m/s also breaks the 0.20 soft bound" reads as **ALL moving cases**. Memory project_zhang2026_failure_mode (2026-05-07, possibly stale) supports 746 (precise-not-soft on 2-4 → only Case 5 breaks soft → line 800 OVERSTATES). ALSO reconcile Case-5 v_f 0.65 (746) vs 0.66 (800). Action: check per-case Zhang v_f in fresh comparison .mat, then narrow 800→Case 5 or broaden 746; comparison table has no v_f column so unadjudicable from tex.
- **SPLIT DECISIONS (user, 2026-06-12):** (1) housing = **in-document `\appendices`** (one PDF, live cross-refs, appendix pages DO count toward TAES length — user accepts less relief for simplicity). NOT a separate supplementary.tex. (2) timing = **COMPILE FIRST** — user compiles current single-file draft, reports page count, THEN Claude cuts exactly what's needed to hit 10 (no gratuitous churn). No local pdflatex on Windows.
- **§IV relocation candidates (ranked by page savings, for post-compile cut):** 4 multi-init Cases 1–4 figs (fig:sup-static/linear/sin/liss, keep Case 5 Circular in main) ▸ 4 baseline velocity-sweep figs (comparison_multi_speed_linear/sin/liss/circular) ▸ 3 IC₂ internal-diagnostic figs (adaptive_gain, sliding, thrust_accel) + funnel_combined figure* ▸ baseline gains table* ▸ mechanism-contrast table* ▸ deep-sweep paragraph ▸ 4 per-baseline subsubsections ▸ summary list (partly restates the observations enumerate). Per feedback_shift_before_delete: MOVE first, dedup later in separate approved pass.
- **APPENDIX SPLIT EXECUTED (commits 4759f5b Stage A, 7c9e785 Stage B; pre-split snapshot Drafts/manuscript_v9.tex).** User reported Conclusion at p.17 pre-split. In-document `\appendices` (A–E existing) + two NEW:
  - **Appendix F "Extended Single-Run and Multi-Initial-Condition Results"**: 4 multi-init Cases 1–4 figs (Static/Linear/Sin/Liss; Case 5 Circular stays main) + 3 IC₂ Case-3 diagnostics (adaptive gain, sliding, thrust) + their paras. 2 main cross-refs repointed.
  - **Appendix G "Baseline Controller Analyses"**: baseline-gains table*, mechanism-contrast table*, comparison_traj3d fig, 4 baseline velocity-sweep figs, 4 per-baseline analyses (\subsubsection→\subsection), Summary-of-Numerical-Observations subsection. 3 main cross-refs repointed (gain table, mechanism table, λ-sweep figs).
  - MAIN §IV now keeps: Tables (UAV params, ICs, multi-init robustness, 5-ctrl comparison table*), figs Circular_3D + speed_envelope (figure) + funnel_combined + comparison_combined (figure*), and the 3-item observations enumerate → Conclusion. check_refs green both stages.
  - Split mechanics: deterministic Python scripts (Write-tool authored, label-anchored block extraction) — NOT manual Edits, NOT Bash heredocs. Gotcha hit & solved: a label string (e.g. fig:sup-liss) also appears in the main cross-ref prose, so anchor block boundaries on `\label{...}`/`\begin{}`, never the bare label.
- **NEXT (resume here):** user RE-COMPILES post-split → report new main-content page count. If still >10: further trim levers (NOT yet cut) = deep-sweep paragraph (§IV.A, dense 33-axis detail → could shrink or move to App G), §I survey compression (handoff lists locked levers), or last resort push funnel_combined/comparison_combined figure*s to appendices (user previously chose KEEP both in main). Also still open: resolve Zhang v_f scope flag w/ .mat (line ~691 Case-5-only vs line in App G per-baseline 0.56–0.66 all-moving; + 0.65 vs 0.66); figure-regen queue (block_diagram epoch-5 labels, Optic→Optical Flow, velocity renames — needs Ubuntu .mat).

**HANDOFF (2026-06-12, last commit 5166617) — superseded above where noted:**
1. **[CLOSED 2026-06-12] abstract rewrite v2 (from scratch, 200 words, gap reworked per user).** User said the old abstract was "not focused" and the first redraft's gap sentence "not good"; v2 below was presented but NOT yet approved/applied. Apply on approval (replace current \begin{abstract} body):
   "This paper addresses soft-precise landing of a low-SWaP-C quadrotor on an unpredictable moving vehicle using only a monocular camera and IMU, without metric depth, heading measurement, or the UAV's own position and velocity. Most prior image-based landing controllers regulate either image features for precise lateral convergence or optical flow for soft touchdown. Regulating both simultaneously is obstructed by the nonlinear coupling between the two image parameters, intensifying near the landing surface, and target visibility carries no formal guarantee. The proposed \emph{Visual Dual-Funnel Adaptive Sliding-Mode Control} (VDF-ASMC) regulates both through two coupled funnels. An optical-flow funnel drives a leakage-type adaptive sliding-mode law that rejects bounded uncertainties from wind, ground effect, and actuator nonlinearities without known bounds. A target feature funnel sizes a state-dependent cone that clips the commanded lateral acceleration, guaranteeing the target remains visible throughout the descent. Lyapunov analysis shows the output error converges exponentially to a small prescribed bound, the key to guaranteed soft-precise touchdown. Across five initial conditions, five target trajectories, and a $\pm 40\%$ target-velocity envelope, VDF-ASMC lands soft-precise on every run, with mean landing time below 18.2~s and horizontal error within 5.8~cm. Against four state-of-the-art baselines, it is the only controller achieving soft-precise landing on every trajectory."
   Notes: 3-deficit gap (features-or-flow / coupling-blocks-both / visibility-unguaranteed) answered in order by S4-5/S6/S7-9; "scale-free" adjective deliberately dropped (S1 carries it concretely) — user may want it back in S4.
2. **Open ruling: "virtual-compass"** (§III.B heading + ~7 uses) — violates virtual-only-for-plane/frame rule; asked twice, no answer yet. Options: named exception, or rename ("compass-free heading integrator").
3. **Next work items (in order):** §III content review (design paras + proofs claim-by-claim — structural pass done, content pass not); §IV review + split to appendix (biggest 10-page win: Cases 1–4 figures, baseline analyses + gains table, 4 sweep figures, mechanism-table candidate); first compile on user side to measure pages vs 10-page target; abstract-consistency check after v2 lands.
4. **MATLAB/PX4 alignment task list (user runs sims):** corrected transport kinematics (h_d = ṡ_d + ψ̇ê₃×s + h_rd s; c_h = −ψ̇ê₃×h − (h·ê₃)h − ḣ_d; ẇ/V_dw dependence drops!); p_10 normalization COMPONENT SWAP bug ([res(2),res(1)] order under-reports v-axis error — likely the IC5 v-breach culprit).
5. Working tree: user-side modified multi-init figure PDFs + Drafts/manuscript_v6/v7.tex untracked — DO NOT touch/commit without asking.
6. Key gotcha: Bash-tool heredocs collapse double backslashes (write tex-surgery scripts via Write tool). check_refs.py after every structural edit.

**2026-06-11 session complete (commits 61db22e→0072865):** §II para-by-para review DONE (all 15 paras resolved): matched-disturbance eq (1); epoch-5 naming (see feedback_optical_flow_naming_epoch5); §II.B 3-rung ladder (Virtual Camera Abstraction / Image Features & Optical Flow / Image Kinematics and Dynamics); KINEMATICS CORRECTION (see project_kinematics_correction_2026_06_11 — s-dot/c̃_h/d_h/h_d transport form, MATLAB+PX4 alignment PENDING); beta finite-via-landing-gear; Singhal-1D-recovery clause; h_rd = "desired image radial velocity". §I compressed 1337→1178 words (-12%, para merges cooperation+vision and sensors+paradigms + sentence trims; 6 survey paras now; further levers if needed: gap-para sentence fuse ~6w, P6 ~8w, then only locked lead-in/contributions). NEXT: §III para-by-para review — queued items: r̄_e⊘φ_max unit audit (px vs tangent), Assumption 2 vs unknowns-item-3 attitude residual, Corollary 1 proof algebra under corrected s-dot, block-diagram caption "desired optical flow h_d"→"desired image velocity h_d", remaining "optical flow h" phrasings, theorem-statement Remark-7 refs OK. Also pending: appendix→supplementary-material split (user plan, 21-page draft), figure regen (velocity renames + block diagram), MATLAB/PX4 kinematics alignment + re-run.

**§II reworked (2026-06-10, uncommitted):** Two passes. (Pass 1, review fixes): pixel-unit convention adopted — f added to pinhole eq + virtual-point map (and C_θ→C_φ TYPO in map row 2 fixed; verified vs visualControl_IBVS_adaptive.m rays=[C_nP;f] construction, f=135px, res=[320,240]); s=[^V r̂/f;1] (matches image_feature(V_nP_i/f)); coplanarity/level-platform sentence at image-position eq (tilt residual → d_h); v_rel→^I v_t/b; Singhal connective "Driven by the practical considerations above..."; Assumption 1 "exist...but are unknown". (Pass 2, restructure per Lin2022/Zhang2026/Singhal genre + Manuscript Notes.docx merge): §II intro = "Consider a quadrotor..." scene-setting + 4-frame enumeration (I defined NED for first time); §II.A → "System Description and Quadrotor Dynamics"; §II.B ladder = B.1 Virtual Camera Abstraction / B.2 Optical Flow / B.3 Image Pose (swapped before pose to kill forward refs) / B.4 Optical-Flow Dynamics; l_α matrix+chain rule → new APPENDIX before bib; §II.C.1 = single merged "Unknowns and Perturbations" 5-item list (perturbations: section label retired); Control Objective lead = notes' unpredictability definition + sensor-suite/own-state-denial sentence + β-grows-unbounded in item 1. Notes' "four image parameters"/old virtual naming NOT merged (superseded epochs). §III pending: r̄_e px-vs-tangent normalization audit; Assumption 2 vs item-3 attitude-residual relationship.

**Contribution list FINAL (commit d7396a0, 2026-06-10):** 4 items, division of labor: (1) stability + BOUNDEDNESS of image parameter errors under unknown bounded uncertainties (no prior bounds), chattering-free; (2) CONVERGENCE — performance constraints → exponential convergence of image parameter errors to small UUBs (never "global" — Thm 1/2 have restricted IC sets), tail "functional requirement of soft-precise touchdown"; (3) cone clamp as "closed-form clamp... hard state-dependent bound sized by the measured margin" + "guarantees by construction" (user-vetted after non-heuristic justification from implementation: hard projection, pinhole-derived terms, margin feedback, logged certificate; invariance proof still deferred — never claim a forward-invariance theorem); (4) empirical, 18.2 s / 5.8 cm validated vs .mat. User intent: items state WHAT is achieved; boundedness-vs-convergence split is deliberate, not redundancy.

**Rulings closed 2026-06-10 (commit 6abfd3f, pushed):** (a) abstract sentence 4 "a guaranteed soft-precise touchdown" KEPT — user ruled the Lyapunov-analysis claim is theoretical (contrast: §I contribution 2 + results prose stay empirical-phrased); abstract results sentences rewritten + 5.8 cm; conclusion worst-case corrected to exact 5.71 cm. (b) speed→velocity propagated paper-wide for target-velocity scaling (§IV.B heading, sweep/envelope/robustness-gap prose, 5 captions, conclusion, future work); "speed" KEPT for scalar magnitudes (ESC, terminal relative speed v_f, contact speed, soft speed bound). FIGURE-SYNC consequence: captions now say "Target-velocity sweep" but generated PDFs/scripts (make_plasmc_plots.py speed-envelope fig + comparison multi-speed scripts) may still render "speed" in titles — add to regen queue (data IS on Windows; matplotlib env permitting). (c) RESOLVED 2026-06-10: λ-sweep .mat files ARE on Windows (MATLAB/Multi_init_cond/Datasets/<Traj>_multi_speed.mat); validated 20/20 soft-precise, per-traj mean t_f 17.48–18.18 s, max xy 5.03 cm, max v_rel 0.154 m/s — joint 18.2 s / 5.8 cm claim holds over all 45 runs (multi-init max xy 5.71 cm is the binding value). Abstract results sentences rewritten same day (quality-over-quantity: robustness dimensions instead of run counts, "guaranteed" dropped, 5.8 cm, "quadrotor's initial state"). Abstract sentence 4 "a guaranteed soft-precise touchdown" STILL pending ruling.

**Terminology epochs locked this session (all applied paper-wide):**
- "optical flow" spelling, never "optic flow" (incl. "optical-flow funnel", Theorem 1 "Adaptive Optical-Flow Funnel Invariance") — follows abstract/§I
- "target feature funnel" for p₁ (epoch 3; never "target image funnel"/"image feature funnel")
- "virtual" reserved for *virtual image plane* + *virtual camera frame* only → "image position", "image orientation", "image point", "Image Point PID", "Image Pose" (epoch 4)
- "the two image parameters" abstract granularity; kinematics-vs-dynamics force litmus

**Open questions for user:**
1. "virtual-compass" (yaw integrator, 8 occurrences) — strictly violates the virtual rule but isn't an image parameter; kept pending ruling.
2. S1-D pose-based PID recap was the only supplement piece omitted (standard background); reinstate as appendix?
3. Orphaned bib entries after Table I rewrite (jabbari2014, lee2012, xie×3, salehi2021, paris2020, kamath2026 — salehi2021/xie2016 still cited in Remark 8 ✓; others uncited).

**Figure regeneration queue (needs Ubuntu — multi-init .mat data not on Windows):**
- `scripts/make_plasmc_plots.py`: titles already updated for funnel rename; also need "Optic Flow"→"Optical Flow" + any "Virtual Image" labels checked, then re-render all PDFs
- `block_diagram_v3.pdf`: block labels still say "Virtual Image Position (2)", "Virtual Image Point", "Virtual Image Orientation (9)" + "Optic Flow (5)"
- Check comparison/multi-speed figure scripts for renamed terms

**Uncommitted at session close (user-side, intentionally left alone):** modified multi-init figure PDFs (all 5 `*_combined.pdf`) + `scripts/make_multi_init_plots.py` + `.gitignore` + deleted `References/` working-tree files. Not Claude's changes — ask before committing or discarding.

**Consistency tooling:** `scripts/check_refs.py` validates ref/label/cite consistency of manuscript.tex (all green as of `1c82c45`). No local pdflatex on Windows — first compile pending on user side.

Related: [[feedback_target_image_parameters]] (epoch-4 note), [[feedback_funnel_naming]], [[reference_abstract_gap_claim]] (all §I/Table-I validation evidence + column litmus definitions).
