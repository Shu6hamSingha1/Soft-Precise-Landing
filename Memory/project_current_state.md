---
name: project_current_state
description: "⭐⭐ State as of 2026-06-21 — THE LATERAL WALL WAS A MATLAB GAIN-PARITY BUG (not a fundamental limit). The combined-barrier 'regression' was that PX4 combined ran the BACK-MAPPED soft-config gains (GAMMA 2.0, KAPPA0 0.5, XI2 0.6) — 3-5x too HOT for the combined surface — causing a_u over-aggression -> fly-aways. FIX: auto-align combined-mode gains to MATLAB VDF-ASMC vdf_params (GAMMA 0.4375/0.5, KAPPA0 0.125, XI2 0.2; 22cc732). With manuscript gains the fly-aways vanish. BAKED DEFAULTS (the new manuscript-combined formulation): PLASMC_COMBINED_BARRIER=1, PLASMC_VDS_KF=1, PLASMC_DHD_KF=1, PLASMC_DW_KF=1, chi_r=0.5 (lateral-velocity arrest, PX4-specific divergence from MATLAB 0.85), h_rd=-0.42 (vdf), PLASMC_YAW_ALPHA_FILT=1 (alpha rate-limit+Savgol, kills terminal yaw corruption). IC2 n=5 RESULT: 4/5 sub-meter (|xy| 0.72-0.99m, td_lat med 0.92), h_e 100% bounded, s_e_n converges to 0 (~96% inside p_r, only mild terminal-1/Z breach ratio~1.05 in last 0.3m), vz_med 1.6, yaw e_a ~0.9deg; 1/5 stochastic terminal-1/Z fly-away. PER-AXIS LEVERS — baked: chi_r=0.5(lat vel), manuscript-z(vert, vz~1.6), alpha-filter(yaw). REJECTED with evidence (DO NOT RE-TRY): P2INF chase-lag (worse), a_u-cap COMMIT_AU_MAX (no help, tuning already tamed a_u~92), yaw KF (per-step gate misses gradual drift, savgol wins), Omega_a/Gamma_a>0.5 (windup), SOFT-CONFIG Z on combined (CATASTROPHIC 5/5 fly — z NOT orthogonal to combined lateral). ⏳ OPEN/NEXT: the FINAL IC2-5 GATE (config only tuned/validated at IC2; must confirm IC3/IC4/IC5 hold — harness pattern run_*_icgate_ab.sh). Records NC100-106. ⚠️ harness launches report spurious 'exit 1' (detached) — confirm via pgrep, not the task status. --- prior (2026-06-20, SUPERSEDED framing): terminal 1/Z amplification — the drone descends FINE to ~2m (offset 0.25m, tight) then s_e_n=lat/Z blows up a small residual offset near touchdown → funnel breach → violent ds_d spike (4-6.6) → marker leaves FoV → TARGET_LOST → open-loop fly-away. NOT perception (decode 100% + flow accurate during overshoot), NOT delivery/authority/D1-sign, NOT freeze-s_e_n (0/3, lies to controller). FIX = COMMAND-BOUNDING (PLASMC_COMMIT_DSD_MAX caps |V_ds_d| once committed, s_e_n LIVE): IC1 variance STD 22.89→0.6 (38x), fly-aways 4/5→0-1/5 = WALL CLOSED at centered IC1. BUT IC2-5 GATE FAILED (centered-specific): off-center reps converge then still diverge — the position cap can't arrest the RESIDUAL LATERAL VELOCITY carried from the 2m offset. Next = velocity-aware terminal handling. All knobs default-off, NOT baked. Also this session: combined-barrier ported (parity-clean vs MATLAB except D1/D2/centroid-CBF, default-off, perception-gated at IC2); moment-loom validated (MATLAB 92→95/100, PX4 FLOW_LOOM_DECOUPLE, gain 1.0); CV-KF V_ds validated (corr 0.41→0.52 vs smooth4) + cal-mismatch rescale 0.79; pyramidal-LK ruled INERT (deficit=ArUco decode-availability). Older (2026-06-15): LATERAL gains-only campaign DONE → KP 5->3 BAKED (6ee0f2c). Plateau CONFIRMED from ALL sides: aggressive=converge-but-overshoot, gentle(CBF_LPF_BEFORE)=sluggish-no-converge, both=catastrophic — no operating point converges AND brakes at IC2. CBF_LPF_BEFORE was briefly default-on (d2fe48f) then REVERTED to default-off (b2b9ef9): MATLAB benefit didn't reproduce; the NEW s_e_n<=p_s metric showed it breaches the funnel EARLY at altitude (2-4m) on the centered IC1 descent. Outer-PID per-term decomposition (while s_e_n diverges): NO term pushes outward (all net-inward); failure=MAGNITUDE — every term ×G_s^-1=(1-r^2)p_s/2 collapses 4-7x at the boundary; the INTEGRAL is the casualty (izeta winds up but G_s^-1 nullifies it -> no recovery authority). SEN_RECOVERY_K (bypasses G_s^-1) didn't shrink the breach -> inner loop is binding. ROOT unchanged: flow under-reports velocity ~2.4x -> brake never delivered -> TL = ARCHITECTURE (KLT corner-track/pyr-LK), out of gains scope. KP=3 IC2-5 gate fixed the IC5 catastrophe (27->2.3m). CBF cbf2 fixes committed+default-on. Earlier soft-config bake/NC79 still stands. IC1 investigation CLOSED: reverting CBF_LPF_BEFORE recovered IC1 (TL 4/5->1/5, breach moved 2-4m->terminal ~1m); residual = terminal perception wall."
metadata: 
  node_type: memory
  type: project
  originSessionId: a377a083-d63b-447a-908e-12017cf609f1
---

## 🔵 2026-06-16 — INNER-LOOP / FLOW thread: ceiling beatable but not a cure → combined sliding variable is next
Deep narrowing of the lateral wall ([[feedback_inner_loop_velocity_thread]]). The LK flow ceiling (~1 rad/s, SATURATING sensor) is BEATABLE via the centroid-rate (`FLOW_CENTROID_RATE`=1 → h/true 0.37→0.96) but accurate velocity ALONE does not cure (still 5/5 TL). Both pillars (CR=0.5 + `SEN_RECOVERY_K`) cut TL 5/5→2/5 but not cured; `a_u` points OUTWARD (h_d rotation-FF-dominated). `PLASMC_CH_CLEAN` (consistent c_h) is smoother + NON-regressing (corrects MATLAB convention-mixing) but not the wall-breaker (baseline a_u already 32% inward). **Every signal-side fix ruled out as a sole cure → the one untried lever is the COMBINED SLIDING VARIABLE `σ=ζ_h+λ·ζ_s` (best `ζ̇_s+λ·ζ_s` with centroid-rate ζ̇_s) — direct position authority, escapes both the ceiling and the cascade-decoupling. Lyapunov re-derivation, MATLAB first.**
- NEW default-off knobs: `FLOW_CENTROID_RATE`, `PLASMC_CH_CLEAN`, `PLASMC_SEN_RECOVERY_K`, `PLASMC_DSD_LAT_MAX`, `FLOW_KF_Q/R`, `FLOW_COND_REJECT` (smart EKF-flow smoothing via lstsq-condition outlier rejection), `PLASMC_PS0_MARGIN`. `N_xy=0.1` neutral (keep 0.02). theta_cap MOVED OUT of cbf2_filter → controller (d3dfbaa; CBF now pure-visibility; validator 12/12). CoG-FF PX4 port = N/A.
- Tests logged in parameter_record.ods **NC80–87**. Commits this thread: ca920f6/6ee0f2c/e71cc89/e01d627/b2b9ef9/e8c2a23/81f96d3/15cd430/294bfb4/19604c2/75e8765/d3dfbaa.

## 🔵 2026-06-15 — plateau CONFIRMED all sides + CBF_LPF_BEFORE reverted + outer-PID decomposition
- **`CBF_LPF_BEFORE` briefly default-on (d2fe48f) → REVERTED to default-off (b2b9ef9, pushed).** MATLAB soft-touchdown benefit did NOT reproduce in SITL. NEW metric `s_e_n ≤ p_s` (funnel residency + breach altitude): LPF-before breaches the funnel EARLY at altitude (2–4 m, all 5 IC1 reps, 4/5 TL) vs LPF-after holding to terminal ~1 m (2/5 clean). Also lateral wash at IC2 + catastrophic with KP↑ (xy 15–21 m). Knob kept (=1 re-enables).
- **Plateau confirmed from EVERY side:** aggressive=converge-but-overshoot; gentle(LPF)=sluggish-no-converge; both=catastrophic. No operating point converges AND brakes at IC2.
- **Outer-PID per-term decomposition while s_e_n diverges:** NO term pushes outward (all net-inward: P −0.27…−0.34, I −0.004, D −0.05, FF −0.05). Failure = MAGNITUDE: every term ×`G_s⁻¹=(1−r²)p_s/2` which COLLAPSES 4–7× at the boundary. The INTEGRAL is the casualty — izeta winds up (→2.4/clamp 5) but G_s⁻¹ nullifies it (I-term 0.006 vs ~0.04) → no recovery authority. Per-term confirmation of the barrier collapse. `SEN_RECOVERY_K` (bypasses G_s⁻¹) didn't shrink the breach → inner loop is the binding constraint, not outer authority. See [[feedback_lateral_overshoot_root]].
- **IC1 investigation CLOSED:** the centered-descent fly-aways were SUBSTANTIALLY the LPF-before default. IC1_Confirm_LpfOff (default-off, n=5) RECOVERED IC1: **TL 4/5→1/5, breach moved from early-altitude (2-4m) to TERMINAL (0.6-1.4m), all converge deep (offmin 0.00-0.09).** Still 0 soft/precise (xy mean 9.1, one terminal catastrophe rep2 rel_vel 16.8) = residual terminal close-range perception wall (architecture). Revert vindicated.
- **Standing conclusion unchanged:** binding constraint = inner-loop velocity under-report ~2.4× = ARCHITECTURE (KLT corner-track / pyr-LK), out of gains scope.

## 🔵 2026-06-14 — LATERAL gains-only campaign DONE (KP 5→3 BAKED), gains plateaued
On the committed-CBF baseline (CBF cbf2 fixes default-on, separate chat). IC2 n=5 sweeps + IC2-5 n=3 gate:
- **`KP 5→3` BAKED (6ee0f2c).** Gains winner: IC2 arrival vel 3.4→2.8, peak vel 8.1→3.8, xy 22→8.6, h_d 2.16→1.27, convergence intact (KP=2 sluggish flt 24s). IC2-5 gate: drift down, **IC5 catastrophe FIXED (27→2.3 m)**, no systematic regression, but still TL-dominated, SP 0.
- **Two diagnostics added, BOTH default-off + kept:** `PLASMC_DSD_LAT_MAX` (ds_d cap, ca920f6) + `PLASMC_SEN_RECOVERY_K` (escalating funnel-breach recovery, 6ee0f2c). The recovery counters the back-mapped barrier's demand COLLAPSE at the boundary (demand g(r)=(1−r²)log((1+r)/(1−r)) peaks r≈0.65, frozen by ratio-clamp |r|≤0.95). **A/B: recovery did NOT reduce breach depth (rmax 3.6→5.2) → escalating the DEMAND doesn't help → bottleneck is the INNER LOOP, not outer authority.**
- **DEAD-ENDS:** PS0 funnel-widen (raises demand ∝p_s, fights KP-down); KD 0.8/1.0 (no TL removal); XI2_xy=0.8 (stochastic, raised arrival vel).
- **ROOT triangulated:** s_e converges deep inside p_1 (r_min 0.1–0.3) then OVERSHOOTS out; the controller commands the inward brake 100% of the breach window but the flow UNDER-REPORTS velocity ~2.4× (meas/GT≈0.40) → brake never delivered → marker leaves FoV geometrically → TL. No gain removes it. **Fix = inner-loop velocity observability (KLT corner-track / pyramidal LK) = ARCHITECTURE, out of gains-only scope.** See [[feedback_lateral_overshoot_root]].
- **⏳ Pending follow-ups:** log NC# rows in parameter_record.ods (KP-down sweep / gate / recovery A/B); n≥10 IC2 confirm of KP=3 (skipped per user "bake now"); push commits (ca920f6/6ee0f2c local).

---
**Status as of 2026-06-12 ~00:30 (commit c5f9989 + records ebdcb47/96dd1fb).**

## Baked config (code defaults now)
- **The IC=2 gain-chain stack ([[feedback_ic2_lateral_gain_chain]]), all baked:** KD_XY=0.5, KI_XY=0.1, XIS_XY=0.5 (γ_s), PSINF_XY=0.35, XI2_XY=0.6 (γ₂), KAPPA0_XY=0.5, LANDING_REF_RAD_OPT_FLOW=−0.25. z-axis untouched (XI2_Z=0.2, KAPPA0_Z=1.0, E_Z=1.0).
- Also baked earlier: **RING_LOOM_NCORN=4** (z ring-loom — LOWERED from 12 on 2026-06-13: GT-loom recheck found =12 was CAUSING the measured-h_z-vs-GT under-report — n_corn collapses to ~4 by 4 m alt so the n_corn≤12 gate fired the ring-handoff the WHOLE descent, but ring loom~0 at altitude → fused pinned ~−0.15 vs GT, 16× under-report; |fused|/|GTloom| alt 1-4m: =12 6% · =0 65% · =4 87%; =4 keeps corner at altitude, ring only at true terminal n_corn≤4. See [[feedback_terminal_descent_loom_overreport]]), guard #1 (PLASMC_FEAT_FOV_CLIP=1), controller log-persistence across re-inits (162b86e), IMG_RECORD video (+5 s tail via IMG_RECORD_TAIL_S).
- **LOOM-EKF FIX baked 2026-06-13 (commit 4ac539f):** the terminal fused loom was FREEZING on a stale value (wrong-sign h_e_z → z-SMC commanded MORE descent). Root: at the terminal corner_ok=False (decode-fail) AND ring_ok=False (lstsq ill-conditioned→zeroed) → EKF random-walk holds last value; and the robust `pure_div` divergence was logged but never fed to the EKF. Fix: (1) `RING_LOOM_PUREDIV=1` feed pure_div as scalar ego-loom when lstsq rejected; (2) `FLOW_HTVZ_PRIOR=1` anchor h_tv[2]→0 (ground target doesn't change altitude; h_tv[0:2] free for moving rover) so the ego-loom resolves to h_tr[2] instead of being parked in h_tv (the −1.06/+1.06 re-freeze); (3) anti-freeze decay backstop. n=1 verified: frozen-run 45→3 frames, h_tv_z +1.06→0, h_tr_z 93% correct-sign. **n=5 IC1-5 gate RUNNING (HtvzGate).** Residual under-report when all sources die = honest 'loom unobservable', curable only by keeping corners decoded (lateral wall). See [[feedback_terminal_descent_loom_overreport]]. ⚠️ NOTE: stale-prox block (IMG_STALE_PROX_EXTENT, half-wired/inert) was accidentally swept into commit 5119af1 by `git commit -- path` semantics.
- **IC1-5 GATE on the loom-fix bake (2026-06-13, HtvzGate):** IC3/IC5 CLEAN (all land close+soft), IC2 4/5 close, **IC1 & IC4 CATASTROPHIC** (balloon+fly-away, 9-20 s flights, 0 SP). A/B (loom-fix ON vs OFF) → loom fix is NOT the cause (IC1 bad both ways); the IC1/IC4 failure is the **lateral close-range wall** (au_xy→86, s_e_n runaway at ~1.7 m via cross(w_i,s)), expressed through an open-loop vertical at corner-death. The IC2-lateral-gain-chain (GAMMA_xy=2.0 etc.) may amplify the wall on clean center descents (GammaXY1 A/B was interrupted by a SITL collision — NOT completed). IC1/IC4 = pre-existing soft-config regression never gated before NC79 (IC2-5 only).
- **DESCENT PERCEPTION = CEILING (2026-06-13, see [[feedback_descent_perception_ceiling]]):** LK/KLT/ArUco/deblur params do NOT improve corner survival or decode — perception is healthy on a clean centered descent (decode 88-100%, corners to deck, marker never fills FoV, frames sharp = no Gazebo motion blur). Failures are fly-away/geometry (lateral wall). **Corner loom RECALIBRATED: `_sensor_cal_hw[2,2] 1.0744→1.2988`** on a clean position-setpoint descent, VALIDATED on a fresh descent (cal/GT 0.89→1.07, corr 0.993; LK-dyn-range nonlinearity caveat). New: `tools/tune_lk_survival.py`, `tools/tune_aruco_decode.py`; clean-descent recorder (`record_output_validation.py` landing + `IMG_RECORD_RAW` lossless dump, `img_node.CONTROLLER_READY` fix).
- **⚠️ UNCOMMITTED (2026-06-13):** RING_LOOM_NCORN default still **4** (user indicated 3 as operating point — NOT baked); FLOW_LK_* env-knobs; IMG_RECORD_RAW dump; record_output_validation CONTROLLER_READY fix; `_sensor_cal_hw[2,2]=1.2988` recal; the 2 new tools. Committed locally only (push blocked — no creds in agent shell): 5119af1 (RING_LOOM 12→4 +stale-prox), 4ac539f (loom-EKF: pure_div+h_tv prior+anti-freeze). NEXT (deferred by user): the LATERAL WALL.
- Marker = aruco_board; 6×6 board cal RESTORED; duty-cycle staleness at legacy defaults (prox-aware variant half-wired, inert).

## ✅ VALIDATION DEBT CLEARED (2026-06-12, `BakedStack_IC{2,1}`, `tools/analyze_baked_validation.py`)
n=5 each, headless, baked defaults. **IC2(2,0,5): SP 0/5, 1 TL, xy mean 8.42 (min 1.60). IC1(0,0,5): SP 0/5, 1 TL, xy mean 3.55 (min 1.91) — regresses pre-bake 1.46.** **Verdict: the chain WORKS in approach** (`approach_min|s_e_n|` IC1 0.00–0.03, IC2 0.07–0.27) **but every rep blows up at the TERMINAL WALL** (GT_alt 0.9–2.7 m, 58–93% through descent; a_u_xy→1087). The big final xy is the wall, not the gains. **Don't revert the bake** (it improved approach); IC2-5 gate NOT run (no winner to promote). Full finding: [[feedback_ic2_lateral_gain_chain]] item 9.

## COMBO CANDIDATE (2026-06-12) — beats baked at single-axis IC, but FAILED the IC2-5 gate
`KD=0.5 + XI2_xy=0.6 + KP=5` on a MATLAB-gains base (E=1/P=1.5/KAPPA0=.125/GAM_Y=.5/N_z=.05/KR=1.5/h_rd=−0.42). Single-axis head-to-head n=5: **IC1 3.16 vs baked 3.55, IC2(2,0,5) 3.85 vs baked 8.42, no catastrophes** — because **baked KP=9 is OVER-GAINED** (amplifies the ungainnable spurious-flow event into a catastrophe; KP=5 halves the demand → cascade bounded a_u 625 vs 1221 → recovers). **BUT IC2-5 GATE FAILED (n=5): 14/20 land, 6 TL (30%), SP 0, 27.45 m catastrophe at IC5.** The single-axis win did NOT generalize; **KP=5's containment is ALTITUDE-DEPENDENT** (held at 5 m, failed at 3 m IC5). One genuine SOFT (IC2 0.75 m, vel 0.20) — the session's only one. **NOT promotable.** Lesson reaffirmed: terminal perception wall is the binding constraint, worse at low-alt/opposite-side. See [[feedback_ic2_lateral_gain_chain]] item 10.

## 🟢 SOFT-CONFIG BAKED (2026-06-13) — first soft+precise of the campaign
On top of the Combo, baked (`controller.py`): `XI2_z=0.6 · P2INF_xy=1.5 · OMEGA_xy=0.1 · GAMMA=2.0/2.0/1.0 · E=0.8/0.8/0.1 · N_z=0.1 · KAPPA0=0.5/0.5/1.0`. n=5 IC2(2,2,5): **3 clean reps soft-precise — xy 0.85/1.28/1.50, vel 1.66/2.44/1.78 m/s** (vel HALVED from 2.84; first soft+precise) + 1 lateral-wall TL (rep3 xy 37.84, κ_z=3.0 = symptom not cause) + 1 flake. **KEY LEVERS:** `KAPPA0_Z=1.0` (the bootstrap value — Combo had reverted it to 0.25) = z braking authority from t=0 → soft touchdown; `E_z=0.1` = crisp arrest but un-bounds κ_z ALONE, stable only PAIRED with κ0_z=1.0 (no ratchet); `GAMMA_xy=2.0` = the un-saturated reaching gain (κ_0/E are sat-gated, can't speed h_e); `N_z=0.1` faster κ_z adapt. The hard-touchdown gap (h_rd/1-Z velocity overshoot) is what this fixed. ⚠️ NOT gate-clean (the TL is the config-independent lateral perception wall). Working baseline. ODS NC77. See [[feedback_plasmc_two_task_framework]].
**Post-bake (NC78-79):** also baked P2INF_z=0.5 (vel→0.37, tightest h_e_z) + h_rd=−0.3 (⚠️ lateral cost: slower descent → longer wall exposure) + KAPPA_MAX_Z 3→10→**REVERTED to 3.0** (gate confirmed 10 net-negative: κ_z ran to 10 in drift reps). **IC2-5 GATE on this config (NC79, n=5, 19 valid): 17 land/2 TL/SP 0 — MAJOR improvement vs Combo gate (14/20, 6 TL, 27m): TL 30%→~10%, IC5 catastrophe cell FIXED (4/5 land, vel 1.36-2.19 soft). NOT clean: SP 0 (xy 1.5-3.2 too loose, vel 2.2-4.9), 2 TL = lateral wall.** Current baked: XI2=.6/.6/.6, P2INF=1.5/1.5/0.5, OMEGA=.1/.1/.025, GAMMA=2/2/1, E=.8/.8/.1, N=.02/.02/.1, KAPPA0=.5/.5/1.0, KAPPA_MAX_Z=3.0, h_rd=−0.3. Remaining gap: precision + lateral wall (architecture). See [[feedback_descent_softness]].

## 🧭 TWO-TASK FRAMEWORK ANALYSIS (2026-06-13) — see [[feedback_plasmc_two_task_framework]]
Soft-precise = `s_e<p_1` (Task 1) AND `h_e<p_2` (Task 2). 30-rep measurement: **Task 2 MET (100%, h_e rides p_2 boundary), Task 1 FAILS (62%)**. The outer barrier-PID is feedback-linearizing (provably `s_e→0`) but only if `ṡ_e=ds_d`; it isn't (`ṡ_e≈ds_d−h_e`), so the guarantee breaks on the unmodeled `h_e` — NOT a gain problem. `h_e` can't reach 0 because the SMC saturates on a huge `h_d` (85-98% the rot-FF `cross(w_i,s)`, w_i spikes ~5 rad/s at close range). Param roles corrected: **Gamma=reaching, E=boundary-layer, Omega=surface slope, P=κ-leakage**. GAMMA=1.0+E=1.5 (NC76) calmed the middle loop (|σ| 3.7→1.92, a_u 770→30) but didn't fix Task 1. De-rotation VERIFIED CORRECT (the "under-reporting" was clock-skew). **#1 LEVER = descent-gate** `h_rd·g(|s_e_n|)` (reference governor: shrink terminal w_i→h_d→SMC reaches→h_e→0→restores `ṡ_e=ds_d`→s_e holds in p_1). Then Gamma↑, then E↑. NOT outer gains / p_2 floor (P2INF doesn't bind, NC75) / Omega↑ (dead-end).

## Open levers (ranked — ARCHITECTURE / reference-governor, not SMC gains)
0. **Descent-gate (reference governor)** — the framework-grounded #1; restores the outer-loop convergence guarantee by keeping `ds_d` feasible. Prototype + n=5 IC2+IC5.
1. **Terminal close-range wall = THE binding constraint.** Re-test the terminal ring-loom (`RING_LOOM_NCORN`, prototype f194989) TOGETHER with the baked chain — the chain's better approach convergence may relieve the lateral fly-aways that made ring-loom "not a net win" before. + keep-marker-decoded (KLT corner-track) + structural ḣ_d. See [[feedback_terminal_descent_loom_overreport]] [[feedback_lateral_kappa_runaway]].
2. **PSINF_XY 0.35→0.6 — TESTED DEAD-END (2026-06-12, n=1 IC2).** Cascade analysis showed `zeta_1` (outer barrier) saturates first terminally → hypothesized the funnel floor was binding → 0.6 to keep it linear. FALSIFIED: terminal `s_e` re-grows to ~1.5 rad (>> any floor) → `zeta_1` still pins at 3.66, outcome unchanged (2.38 vs 2.23 m). The binding quantity is the terminal `s_e` magnitude (= off-screen virtual centroid = perception wall), not the floor. See [[feedback_ic2_lateral_gain_chain]] item 7. No outer-funnel gain fixes the terminal wall.
3. **Structural ḣ_d fix** — dh_d spikes = numerical rate of the GENUINE cross(w_i,s) feedforward (gyro-verified). USER RULE: do NOT cap (DH_D_MAX/DW_MAX stay 50/30); fix = smooth/analytic ḣ_d or gyro-sourced w_i.

## Key methodology rules learned this session
- Check r=s_e_n/p_s before believing any "damped hold" (saturated crawl ≠ damping).
- a_u≈−ḣ_d at kicks → decompose h_d terms before blaming a gain.
- Reject summary rows with flight_s≈0/empty GT (NameError episode).
- The user drives gain decisions; one knob per run; full-flight logs (the re-init wipe fix) are what made the chain diagnosable.
