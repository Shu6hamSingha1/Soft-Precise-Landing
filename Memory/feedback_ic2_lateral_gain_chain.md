---
name: feedback_ic2_lateral_gain_chain
description: "IC=2m lateral gain-tuning chain (2026-06-11 evening, user-driven, n=1 per step with full initial-segment logs). FIVE gain defects found in sequence, each confirmed by data: (1) middle funnel too lazy (γ₂=0.2 vs MATLAB 1.2 → σ at 5-20% of E → SMC ASLEEP all approach, κ leaks 0.156→0.06); (2) κ unarmed at crossing (KAPPA0_XY=0.156); (3) outer funnel overtakes the error (γ_s=1.0 → ratio saturates → G_s⁻¹∝p_s → DEMAND STARVATION — the combo's 'nice damped hold' was a saturated crawl, NOT damping); (4) outer integral 10× MATLAB (K_ri=1.0 vs 0.1) → ∫ζ_s≈−2.4 windup pushes ds_d=+1.4 THROUGH center → overshoot. (5) K_rd=0 vs MATLAB 0.5031 → no phase lead → structural overshoot; KD_X=0.5 gives a damped ring-down (overshoot 5× smaller). Candidate stack (env-only, NOT baked): KD_X=0.5 + KI_X=0.1 + XIS_X=0.5 + XI2_XY=0.6 + KAPPA0_XY=0.5 + h_rd=−0.30."
metadata:
  node_type: memory
  type: feedback
  originSessionId: 7415f420-9591-41b1-8349-bb9361a8dc82
---

> ⛔ SUPERSEDED/CORRECTED 2026-06-26: The outer back-mapped-PID gains tuned here (K_rd/K_ri/XIS/XI2) are no longer in the lateral path (the combined surface drops ds_d); 'architecture not gains / KLT' is obsolete. The methodology (check r=s_e_n/p_s; de-rotation verified correct) endures. The PX4 lateral "wall" was a gain-parity bug + the velocity-damping lever (tighten the lateral flow funnel XI2_xy), NOT a perception/architecture/inner-loop-velocity limit; the combined sliding surface σ=ζ_h+χ_r·ζ_r is baked default-on and gives 10/10 bounded landings. The residual is a terminal SOFT velocity kick (≈38ms lag), not a precision wall. See [[feedback_flow_funnel_zetah_works]]. Content below kept as history.

**Thread (user insisted on GAIN tuning, not patches — and was right at every step).** IC=(2,0,5), single runs with the new full-log persistence; each step's diagnosis from the ACTUAL initial-segment data.

## The four gain defects, in causal order
1. **Middle funnel too lazy → SMC asleep during approach.** `p_2_0=25, γ₂=0.2` keeps `r=h_e/p_2` ≤0.2 even for h_e=−3.9 → σ at 5–20% of E=1.5 → `sat(σ/E)`≈0.05–0.2 → the SMC term ~0 AND the κ-ODE (gated by |σ|·G, G∝1/p₂) never grows — **κ LEAKS 0.156→0.06 during the approach** (un-adapts exactly when needed). MATLAB parity gap: `gamma_2=[1.2,1.2,1.5]` (funnel tight in ~2 s) vs the port's 0.2 (≥10 s). **Fix: `PLASMC_XI2_XY=0.6`** → approach closes 2→0.04 m in 3 s (was: never).
2. **κ unarmed at the center-crossing.** With σ awake, the crossing (~0.7–1.2 m/s) still overshot because κ had leaked to 0.11. **Fix: `PLASMC_KAPPA0_XY=0.5`** (same medicine as the z-bootstrap) → crossing damped (2.16→0.51 held, no swing) in the combo run. NOTE: κ₀ alone (without XI2) does NOTHING — sat≈0.05 gates it.
3. **Outer funnel overtakes the error → demand starvation (a FALSE-damping trap).** At `γ_s=1.0` from a 2 m start, p_s contracts below the still-closing error at t≈1.2 → ratio clamp (S_MARGIN) → barrier saturates AND `G_s⁻¹∝p_s` collapses → **ds_d decayed 4.1→0.35 while the error stood still — the combo's "well-damped hold at 0.5 m" was demand-starved crawling OUTSIDE its funnel, not damping.** **Fix: `PLASMC_XIS_X=0.5`** → funnel stays valid (r −0.5…−0.65) → demand sustained (4.3→3.1) → genuine 1.2 m/s closure.
4. **Outer integral 10× MATLAB → windup pushes THROUGH center.** With real convergence restored, the crossing showed `ds_d_x=+1.42` at `sen_x=−0.06` — decomposition: ~+0.6 P-term, the rest `−K_ri·∫ζ_s` with ∫ζ_s≈−2.4 wound up over the 2 s approach (izeta_clamp=5 never engaged). The port's `K_ri=1.0` (10× MATLAB's 0.1, added to fix slow drift UNDER THE STARVED-DEMAND REGIME) is overshoot fuel once the P-path works. **Fix: `PLASMC_KI_X=0.1`** (MATLAB parity) — TESTED: the windup signature is GONE (demand correctly REVERSES at the crossing: ds_d_x=−0.99 at sen_x=+0.18, vs +1.42 pushing through) — but the n=1 draw still overshot (0.42→3.72→5.29): the ~1 m/s crossing outran the ~1 m/s² brake and the FAR-SIDE funnel saturation (r=+2.5 → demand collapse) killed the recovery. Remaining links resolved by #5 below.
5. **No phase lead → the overshoot is structural until K_rd returns (the 5th MATLAB-parity gap).** Counter-evidence first: `GAMMA_X=1.0` made it WORSE (+10 kick AT the crossing — the middle loop is a velocity TRACKER enforcing a demand that lags; at center the measured flow (+0.33) was below the still-positive lagged demand (+0.66), so a stronger Γ pushed HARDER through). No σ-side gain (κ₀/KI/Γ) can brake before center because `h_d ∝ s_e` flips AT the crossing by definition. The only rate-proportional (lead) element is the outer D-term — **MATLAB flies `K_rd=0.5031`, the port baked 0** (for a CLOSE-RANGE corner-jump spike issue, guarded anyway by DH_D_MAX; the approach-altitude centroid is KF-smoothed). **Fix: `PLASMC_KD_X=0.5` — TESTED: crossing overshoot 5× smaller (+0.6 m vs 2.4–9.5 m) and the FIRST damped ring-down of the campaign (0.47→1.10→0.58 pull-back).** Run still failed at the deck: the ring-down (~±0.6–1.3, ~2 s left) wasn't settled when the descent reached <1 m → the close-range wall (4.41).

## Supporting facts
- `h_rd=−0.30` (vs −0.42): at IC=0.5 it killed the descent-phase over-correction ignition (offset held 0.4–0.6 m to the deck; loom tracked −0.3). At IC=2 it's pacing, not the fix.
- **Controller log-wipe FIXED (162b86e):** mid-flight re-init (visibility loss) used to WIPE the logs — the initial-approach data vanished exactly when the failure happened. Now archived+merged ("record until the controller fails"). This unblocked the whole diagnosis.
- The terminal close-range wall (decode death <0.5 m → phantom burst → hop/drift) is UNTOUCHED by all of this — it ended every otherwise-good run (combo: reached 1.2 m at 0.52 off, then the wall → 5.25).
- Duty-cycle staleness REVERTED to legacy defaults (global-strict regressed: benign mid-flight blips → permanent final-descent aborts from altitude → 21–23 m ballistic). Proximity-aware variant left HALF-WIRED (knobs `IMG_STALE_PROX_*` exist in img_data, handlers NOT switched — inert).

## Post-bake additions (late 2026-06-11/12)
6. **dh_d spike attribution CORRECTED:** decomposing ḣ_d term-by-term, the spikes are ~100% the numerical rate of **cross(w_i,s)** (−154…+93/s), NOT the KD demand-flip (ds_d-rate only ±13–35, the genuine lead). And w_i_z is NOT noise — the FC gyro confirms genuine yaw motion (std 0.7–0.9, peaks 7 rad/s); the term is a real kinematic feedforward whose IMAGE-RATE STEPPING makes its numerical derivative spiky. USER RULE: capping dh_d/dw is NOT the correct solution — the structural fix (smooth/analytic ḣ_d, or gyro-sourced w_i) is the OPEN item. (DH_D_MAX=5 single-run evidence: min 0.28 best-ever at IC(2,2,5), kick 20.8→11.9 — but stays a diagnostic-only knob, NOT baked.)
7. **The post-crossing plateau = the saturated-barrier demand CEILING.** Outside the funnel, ζ clamps at ζmax=3.66 (S_MARGIN=0.05) and G_s⁻¹∝p_s·e^(−ζmax) collapses → restoring demand pinned at G_s⁻¹·KP·ζmax ≈ 0.56–0.80 rad/s REGARDLESS of error size (observed ds_d −0.8…−1.0 exactly at the ceiling; error creeps at ~0.35/s). The PPC barrier escalates inside and GIVES UP outside. Parameter: the ceiling ∝ p_s·K_rp·f(S_MARGIN).
   **PSINF_XY=0.6 → DEAD-END (2026-06-12, n=1 IC2 `PSINF06_IC2`, full-cascade analyzed).** The n=5 BakedStack analysis showed the terminal failure is `zeta_1` (OUTER position barrier) saturating FIRST (→±3.66) as `s_e` re-grows near the deck, which led to the hypothesis that the funnel FLOOR `p_s_inf=0.35` was the binding constraint → raise to 0.6 to keep `zeta_1` linear. **FALSIFIED: the terminal `s_e` re-grows to ~1.2–1.7 rad (NOT the assumed 0.3–0.5), which overwhelms a 0.53–0.71 floor exactly as it does 0.31–0.41 → `r` still 1.9–2.0 (>1), `zeta_1` STILL pins at 3.66, outcome 2.38 vs 2.23 m (no change).** The binding quantity is the MAGNITUDE of the terminal `s_e` excursion (= off-screen virtual centroid under close-range tilt = the PERCEPTION wall), not the funnel floor — no realistic floor contains a 1.5-rad excursion. Only effect of 0.6 was a marginally calmer z-channel (a_u_z 14.7→4.7). **The cascade ordering (zeta_1 saturates before zeta_2; severity tracks whether it propagates to zeta_2/sigma→kappa) is REAL and worth keeping as a diagnostic, but its trigger is upstream perception, not a gain.** S_MARGIN (canonical) and KP (LK-capped) also rejected. **No outer-funnel gain fixes the terminal wall.**
8. **BAKED (c5f9989, user decision):** KD_XY=0.5, KI_XY=0.1, XIS_XY=0.5, PSINF_XY=0.35, XI2_XY=0.6, KAPPA0_XY=0.5, h_rd=−0.25 — now code defaults. DH_D_MAX/DW_MAX unchanged (50/30).
9. **n=5 VALIDATION DONE (2026-06-12, headless, `BakedStack_IC{2,1}`, `tools/analyze_baked_validation.py`) — debt CLEARED, verdict: chain works in approach, terminal wall blocks the landing.** IC2(2,0,5): 5/5 valid, SP **0/5**, 1 TL, xy mean 8.42 (min 1.60, max 19.70). IC1(0,0,5): 5/5 valid, SP **0/5**, 1 TL, xy mean 3.55 (min 1.91) — REGRESSES the pre-bake FeatClip E_z=1.0 baseline (0 TL/1.46). **BUT the failure is the terminal wall, NOT the gains:** every rep's approach converges to near-center (`approach_min|s_e_n|`: IC1 **0.00–0.03** dead-center, IC2 0.07–0.27, the 2 m offset closed — the chain delivers its claimed mechanism at n=5), then a perception blowup at **GT_alt 0.9–2.7 m, 58–93% through descent** (|s_e_n|→1–3, a_u_xy→65–**1087**, κ_xy ratchets) throws it off-target in the last 1–2 s. The big final xy is the wall corrupting the terminal phase, not an approach defect. **DECISIONS: (a) PSINF 0.35→0.6 DEFERRED** — it lifts the demand ceiling, irrelevant when the approach already centers; the binding failure is downstream. **(b) IC2-5 gate NOT run** — no clean winner to promote. **(c) Do NOT revert the bake** — it improves approach convergence; reverting loses that without touching the wall. **Next lever is architecture, not gains:** terminal ring-loom (`RING_LOOM_NCORN`, prototype f194989) + keep-marker-decoded (KLT corner-track) + structural ḣ_d — see [[feedback_terminal_descent_loom_overreport]] [[feedback_lateral_kappa_runaway]]. Infra added this session: `p_s(t)`/`dp_s(t)` now logged in controller `_buildLogDict` (exact funnel residency) + `tools/analyze_baked_validation.py` (validity gate + residency report).

10. **KP=9→5 is a ROBUSTNESS LEVER the baked stack lacks — the "Combo" beats baked at n=5 (2026-06-12).** Built via a KD/XI2/KP n=1 sweep at IC=(2,2,5) on a MATLAB-gains base (E=1/P=1.5/KAPPA0=.125/GAM_Y=.5/N_z=.05/KR=1.5/h_rd=−0.42): MATLAB-canonical KD=1.4375 → **TL** (close-range D-spike doubles the lateral demand → fly-away); **KD=0.5 → lands** (validates the baked KD=0.5 over MATLAB's 1.4375); **+XI2=0.6** centers the approach + calms the control (theta 15 vs 111) but TL'd on a 100%-corner-starvation draw (control healthy, perception lost — cleanest control-vs-perception separation); **+KP=5** (gentler outer demand). The n=1 outcomes were NOT rankable (dominated by the stochastic close-range perception draw — KP lower even *worsened* centering yet landed closer one draw). **The COMBO (KD.5+XI2.6+KP5+MATLAB base) at n=5, head-to-head vs baked at the SAME ICs: IC1 3.16 vs 3.55, IC2 3.85 vs 8.42 — and NO catastrophic fly-aways (worst landed 6.17 m vs baked's 19.70).** Mechanism: KP=5 halves the outer demand, so when the spurious-flow perception event fires (it STILL fires — ungainnable; Combo IC2 rep3 h_x=16.5, theta=295) the cascade reaches only a_u≈625 (vs baked 1221) and the drone RECOVERS to a landing instead of flying away. **So the baked KP=9 is OVER-GAINED — it amplifies the perception catastrophe; KP=5 contains it.** STILL not precision (SP 0/5, 1 TL/5 each — the terminal corner-starvation TL persists). NOT bake-ready (reverts E/P/KAPPA0 to MATLAB). **IC2-5 GATE RESULT (2026-06-12, n=5, `ICValidation`): FAIL — 14/20 land, 6 TL (30%), SP 0, incl a 27.45 m CATASTROPHE at IC5.** Per-IC (gate ICs are DOUBLE-axis 2,2,*): IC2(2,2,5) 5/5 0TL + the session's 1 genuine SOFT (0.75 m, vel 0.20); IC3(−2,2,5 opposite-side) 2 TL; IC4(2,2,7) 1 TL; IC5(2,2,3 LOW-ALT) **3 TL incl 27.45 m**. **The single-axis IC2 win did NOT generalize** (classic [[feedback_ic_validation]] lesson). **KP=5's catastrophe-containment is ALTITUDE-DEPENDENT** — held at 5 m, FAILED at 3 m (IC5 reaches the close-range terminal wall sooner, less time to center). So the Combo is a real *single-axis robustness* improvement + a clean proof that baked KP=9 is over-gained, but it is **NOT promotable** — the off-center/low-alt terminal perception wall persists. Bundles: `MATLABvals_*`, `ComboKDXIKP_IC{1,2}`, `ICValidation/`. ODS NC64-68.

11. **zeta_1 SATURATION ROOT-CAUSE (2026-06-12, all-runs scan + skew-free verification) — it's CONTROL (lateral hold lost during descent), NOT a perception/de-rotation bug.** Scanned all 60 reps: `zeta_1` (outer barrier) saturates because **`s_e_n` RE-GROWS up THROUGH the funnel** (d|s_e_n|/dt = +0.7…+1.5/s at the breach), NOT because `p_s` contracts onto a stalled error (NOT funnel-overtake) and NOT a floor problem (error blows to 1.5–1.7 ≫ any p_s_inf — re-confirms the PSINF dead-end). The arc, identical every rep: error converges INSIDE the funnel to near-center (s_e_n 0.06–0.36) at **~3–4 m alt**, then the drone **PHYSICALLY drifts** laterally away during descent (GT lateral re-grows +0.96…+2.68 m, GT-internal so robust) → s_e_n re-grows + 1/Z amplifies → shoots through p_s(~0.5) → saturate → demand collapses → drift completes to 1.6–3.6 m. **The breach ONSET is ~3–4 m, NOT <0.5 m** (corrects the "terminal <0.5 m wall" framing). **CAUTION — a mid-analysis claim that "the tilt-leveled VIRTUAL centroid UNDER-REPORTS the drift onset (0.07 vs GT 0.52)" was FALSE — a CLOCK-SKEW ARTIFACT** (I index-aligned three different clocks: controller perf_counter, GT Time 0–3.7 s *relative*, Img stamp 15–37 s *absolute* — the exact invalid method [[feedback_imgdata_gt_clock_skew]] warns about, on a +1.5/s signal). **`_getVirtualPts` de-rotation VERIFIED CORRECT** by a skew-free unit test: synthetic tilted camera (roll/pitch 0–15°) viewing a marker at offset Δ/height h → virtual centroid = Δ/h EXACTLY, tilt-invariant. **DO NOT "fix" the de-rotation — it is correct.** The real lever for the drift is **descent-coupled lateral control** (descent-gating / convergence-ordering: slow the descent while genuinely off-center; scale-free) — the lateral hold that works at constant altitude is lost once `h_rd` drives the descent. Method rule reinforced: NEVER cross-clock index-align Img/GT/controller logs (use GT-internal or controller-internal quantities; verify perception claims with skew-free unit tests).

**Why:** the lateral convergence problem was four stacked gain defects, separable only with full-flight logs; each "nice" intermediate behavior needed re-reading against the funnel state (saturated-crawl ≠ damped). KP is a robustness lever: lower KP bounds the perception-triggered cascade's consequence (lands vs fly-away) without fixing the perception itself. zeta_1 saturation = genuine lateral drift during descent (control), de-rotation is correct.
**How to apply:** candidate stack = `KD_X=0.5 + KI_X=0.1 + XIS_X=0.5 + XI2_XY=0.6 + KAPPA0_XY=0.5 + h_rd=−0.30` — ALL env-only, NOT baked; validate KI_X run → n=5 IC=2 → IC1 regression check → only then discuss baking. When reading approach plots, ALWAYS check r=s_e_n/p_s — outside the funnel the demand is saturated/collapsing and apparent calm is starvation.
